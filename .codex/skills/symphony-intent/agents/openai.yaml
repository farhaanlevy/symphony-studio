interface:
  display_name: "Symphony Intent"
  short_description: "Plan and inspect Symphony work safely"
  default_prompt: "Use $symphony-intent to turn this product intent into a dependency-aware proposal, then report the durable local status and any external-broker blocker truthfully."
