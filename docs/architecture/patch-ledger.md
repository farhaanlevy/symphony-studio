# Patch ledger

Ledger revision: `23`

This ledger classifies every non-trivial downstream change. Documentation-only
records may be grouped when they establish one coherent package boundary.

| ID | Package | Class | Surface | Purpose | Preservation / removal evidence | Status |
|---|---|---|---|---|---|---|
| STUDIO-0001 | R0-01 | B | Repository metadata and documentation | Lock target/provenance, route the two specifications, document fork policy, governance, preflight, and threat model. | No runtime behavior changes; root `SPEC.md`, `LICENSE`, and `NOTICE` hashes are recorded in R0-01 evidence. Modified upstream README carries a visible downstream notice. | Accepted |
| STUDIO-0002 | R0-01 | A | `elixir/test/symphony_elixir/core_test.exs`, `orchestrator_status_test.exs` | Make retry scheduling and dashboard semantics deterministic by using the memory tracker, an exact scheduling bracket, explicit render width, and a raw sanitizer oracle. | Test-only; production modules are unchanged and no upstream test is removed or disabled. Both files carry downstream notices. Narrow/wide focused tests, three retry seeds, and full upstream `make all` pass. | Accepted |
| STUDIO-0003 | R0-02 | B | `CODEX_VERSION`, `CODEX_LOCK.json`, generated schema bundle, method/field matrix, schema loader, and verifier | Pin the exact Codex 0.144.3 App Server contract and make stable/experimental protocol presence machine-verifiable without inventing runtime capabilities. | Raw Codex-generated files are retained. JSON uses a documented semantic hash because aggregate definition order is nondeterministic; TypeScript uses raw hashes. Clean regeneration, exact executable checksums, matrix assertions, and the package-local loader pass. | Accepted |
| STUDIO-0004 | R0-02 | A | Strict fake App Server, stateful loopback fake Linear, network-hermetic test startup, supervised test lifecycle, and `make schema` | Replace weak line-count/external-network/lifecycle assumptions with reusable ordered protocol fixtures, a real-client loopback boundary, and deterministic supervisor ownership. | Production tracker and orchestration behavior is unchanged. Ordinary tests cannot address the real Linear endpoint; the separately gated live E2E opts in explicitly. Strict method/ID/schema, mismatch, isolation, pagination, blocker, mutation, uncertainty, lifecycle stress, and full-suite gates pass. | Accepted |
| STUDIO-0005 | R0-02 | A | `Config.Schema`, Codex App Server startup requests, and dynamic-tool declarations | Reconcile production outbound requests, sandbox policy, callback decisions, and dynamic-tool responses with the pinned Codex 0.144.3 generated contract. | Initialization order and Symphony orchestration remain intact. Exact fixtures prove the request and response envelopes; legacy `reject` policy normalizes to granular booleans, unsupported policy shapes fail closed, `never` cannot approve callbacks, and only pinned sandbox variants reach the wire. | Accepted |
| STUDIO-0006 | R0-03 | A | `Codex.Connection`, `JSONLFramer`, `RequestPolicy`, `StderrDiagnostics`, `TransportError`, `AppServer`, configuration, and strict App Server fixtures | Replace line-buffered mixed transport assumptions with strict bounded JSONL, separate bounded stderr, exact request correlation, absolute deadlines, method-classified overload handling, and conservative uncertainty for side-effecting requests. | The pinned request shapes and original App Server workflow remain. Focused tests cover fragmentation/coalescing, messages beyond one MiB, the 16 MiB decoded-frame limit, malformed/contaminated stdout, duplicate/unexpected/late responses, initialization order, bounded `-32001` retry, terminal acknowledgement races, content-free prompt-safe diagnostics, and no blind side-effect retry. The final source-bound transport suite and complete upstream gate pass. | Accepted |
| STUDIO-0007 | R0-03 | A | `ProcessAdapter`, `CleanupGuardian`, `ErlexecRuntime`, application supervision, and vendored `erlexec` 2.3.4 | Execute the App Server argv without a shell and prove synchronous local containment across owner death, cancellation, timeout, hostile descendants, PID reuse, and cleanup-helper failure. | OpenAI Symphony remains the runner and the target command contract remains argv-based. The per-attempt containment launcher/target bootstrap starts empty, exact PID-namespace root/target identities are captured before target release, target environment is bounded and target-only, and cleanup retains independent pidfd authority over the namespace root and anchored outer group. The long-lived vendored `exec-port` remains an upstream trusted-runtime boundary; its build-only patches are documented. Deterministic, adversarial, 100-iteration cleanup replays and the complete gate pass. | Accepted |
| STUDIO-0008 | R0-03 | A | `AgentRunner`, `Orchestrator`, `SSH`, configuration, and the remote live-E2E gate | Fail closed after unverified cleanup and prevent unsupported remote-worker configuration or hot reload from reaching an SSH/process dispatch path. | Scheduler admission, retry, and tracker semantics remain upstream-compatible for valid local workflows. Remote execution is explicitly release-gated rather than partially implemented. Startup, reload, retry, stale-state, no-SSH, and deferred-remote-E2E tests pass. | Accepted |
| STUDIO-0009 | R0-03 | B | `CompatibilityCircuit`, AgentRunner/Orchestrator admission, safe status metadata, compatibility-manifest transport evidence, schema tooling, release documentation, third-party provenance, and threat register | Persist a source-identity-bound dispatch circuit after protocol incompatibility, bind the focused conformance result to the pinned Codex/schema/source seal without prematurely claiming R0-06 runtime capabilities, and record the local Linux/erlexec security boundary. | The compatibility marker is workspace scoped and mode `0600`; the full per-operation ledger and reconciliation reducer remain R1-07. `fixtures` and `transportConformance` are `pass`; `runtimeCapabilities` remains `not_run` and overall compatibility remains `pending_r0_06`. Strict validation rejects stale/mixed circuit or evidence states. The accepted seal binds 113 files and 276 tests to source SHA-256 `fc44e9692d6f443256ee304ae7ccd9df8e7267eaa1e5d453648956143eb47f91`; the complete 398-test upstream gate and independent reviews pass. | Accepted |
| STUDIO-0010 | R0-03 | A | Root `.gitattributes`, `elixir/Makefile`, and `elixir/mix.exs` quality-gate configuration | Keep the full gate executable across supported OTP warning forms, type-check the vendored runtime contract, preserve byte-exact third-party inputs, and make structural line-coverage boundaries explicit without suppressing analysis findings. | Dialyxir 1.4.7's crashing `short` warning formatter is replaced by Dialyzer's native formatter as the durable gate policy, not a version-scoped workaround; warning analysis and nonzero exit semantics remain unchanged. `:erlexec` is added to the PLT, no warning ignore is configured, the coverage threshold remains 100%, every R0-03 structural exclusion has direct/adversarial tests, and the complete gate reports zero Dialyzer errors and 100% of measured modules. Git whitespace diagnostics exempt only ten unchanged upstream erlexec files with byte-preserved legacy whitespace; downstream-authored and patched vendor files remain checked. | Accepted |
| STUDIO-0011 | R0-04 | A | `Identity`, canonical `Event`, injected `EventSink` with Noop/Memory adapters, AgentRunner/Orchestrator lifecycle propagation, Presenter additive correlation, and focused tests | Add stable run/attempt/event identity and an optional normalized event boundary without replacing Symphony's scheduler or requiring Studio persistence. | Noop retains nothing by default; Memory replay is bounded and process-local with per-run FIFO retention and deterministic run eviction by oldest successful new-event append. Existing callbacks and API fields remain intact, stale attempts cannot regress the live projection, sink history loss cannot block safety state, and durable authority remains R1-01/R1-02/R1-07. The 308-test source-bound seal, complete 432-test gate, and independent adversarial reviews pass. | Accepted |
| STUDIO-0012 | R0-04 | A | `Codex.Connection`, `Codex.AppServer`, logical operation correlation, and regression tests | Assign a UUIDv4 identity before each logical outbound operation and carry it through acknowledgement, bounded wire retry, failure, and uncertainty. | JSON-RPC request IDs and canonical request hashes retain their original roles; identical intentional calls remain distinct; uncertainty is attributed to the actual operation; private parameters are excluded; and the durable operation ledger remains R1-07. Retry/uncertainty regressions and a real initialize-only pinned-Codex smoke pass. | Accepted |
| STUDIO-0013 | R0-04 | B | Package checkpoint, journal, threat register, Build Week delta, behavior docs, and source-bound compatibility evidence/tooling | Record the Studio release boundary and bind the new event/idempotency contracts to deterministic evidence without claiming later durability or readiness. | The accepted manifest retains `runtimeCapabilities=not_run` and `overall=pending_r0_06`; its 122-file/308-test source seal, complete upstream gate, no-model smoke, hygiene checks, and independent reviews pass. | Accepted |
| STUDIO-0014 | R0-05 | A | `RuntimeSupervisor`, registered `Codex.CleanupBarrier`, `CleanupSupervisor`, `ConnectionSupervisor`, `Codex.CleanupGuardian`, `Codex.Connection`, `Codex.ProcessAdapter.StartupCleanup`, AgentRunner cancellation/authority retention, `Codex.AppServer` turn interruption, WorkspaceHookRunner containment, Orchestrator terminal/retry reconciliation, and lifecycle regressions | Make cancellation converge before hooks or workspace deletion, keep exactly one cleanup actor authoritative through ambiguous guardian startup, ordinary teardown races, nested-supervisor crashes, runtime child crashes, and startup rollback, prevent a restarted in-memory orchestrator from coexisting with predecessor connections, agents, or hook tasks, bind cleanup to the workspace that actually ran, and make retry removal timer-safe. | The upstream orchestrator remains scheduler authority. The `:one_for_all` runtime starts the cleanup registry, cleanup-task supervisor, and direct Connection supervisor before hook/agent supervisors and Orchestrator; restart intensity is bounded to 10 restarts in 5 seconds. A guardian acknowledges readiness only after trapping exits and monitoring its owner; its token-matched handle carries durable verification state and is registered with the cleanup barrier. Guardian start errors after invoking Task.Supervisor are treated as ambiguous because a child may already exist: the starter waits for that exact ready token, adopts the child if it acknowledges, and otherwise holds fail closed forever. Only positively observed non-start or known child absence can transfer authority toward caller-owned cleanup; an ambiguous result never falls through to inline cleanup. `CleanupBarrier` and `CleanupSupervisor` are reciprocal shutdown barriers if either one crashes. The barrier also registers and monitors every established Connection, AgentRunner, and WorkspaceHookRunner lifetime, so killing their nested Connection/Task/WorkspaceHook supervisor cannot permit a replacement runtime to coexist with a trapped predecessor member. Connection never performs an ordinary physical adapter stop: its first teardown request delegates to `CleanupGuardian.request_cleanup_once/2`, and only the guardian's bounded worker invokes `adapter.stop`; the established handle is published before readiness and marked durably verified before success is observable, while failure commits `cleanup_authority: :guardian`, and every later close, owner-down, supervisor-exit, child-exit, failure, or terminate path observes that handoff. Barrier-registration loss likewise keeps the acknowledged guardian as the sole actor while `CleanupSupervisor` survives; if both runtime authorities are unavailable, guardian cleanup must become durably verified before startup raises and any caller-owned inline fallback can run. AgentRunner and hook runners retain and monitor startup-cleanup handles, accept a late exit only when durable status proves cleanup, and otherwise hold the runtime closed. A cooperative stop requests `turn/interrupt`, retires the App Server connection, worker, hook runner, and hook descendant, runs `after_run` once, and returns the attempt-bound workspace. Cancellation failure or timeout never synchronously terminates the infinite-shutdown controller: a live retained PID remains safety-blocked and consumes global, immutable admitted-state, and worker-host capacity until exit. Terminal turn events clear stale interrupt targets. The exact binding is published before hooks; same-sender lifecycle events preserve ordering; unverified cleanup preserves the claim/workspace without retry. Terminal cleanup revalidates that exact path/root before `before_remove` and deletion, and every removed retry entry first cancels its timer. Durable claims, crash-window operation reconciliation, and repeated-failure circuit breaking remain R1-07/R1-12. | Accepted |
| STUDIO-0015 | R0-05 | A | `Workspace`, `WorkspaceHookRunner`, `PathSafety`, workflow/config root normalization, managed sandbox narrowing, remote-worker gates, behavior documentation, and path/hook tests | Preserve per-issue isolation while making identifier mapping, symlink handling, hook execution, sandbox roots, and cleanup fail closed at the local host boundary. | Ordinary safe identifiers and the upstream per-issue layout are retained; repaired or truncated identifiers receive a deterministic digest suffix. Relative roots bind to the selected `WORKFLOW.md` directory and are canonicalized once. Issue leaves must be direct non-symlink children; bound cleanup revalidates the captured canonical root around hooks. Local hooks receive an explicit environment, bounded content-free output accounting, supervised descendant cleanup, and retained cleanup authority; failed creation rolls back only a new workspace, never a reused one. The userspace deletion sequence is scoped to the supported one-trusted-operator, non-hostile-same-UID R0 profile. Upstream sandbox resolution remains available, opt-in managed mode binds writable roots to the exact issue workspace, and remote work remains gated to Release 5. | Accepted |
| STUDIO-0016 | R0-05 | A | `Codex.DynamicTool`, pinned Absinthe 1.11.0 parser dependency, and raw `linear_graphql` contract tests | Enforce the root Symphony contract that one raw Linear tool call contains exactly one query or mutation without replacing the existing high-trust tool. | Default upstream mode still accepts one raw query or mutation, including its original fragments and directives, and uses the configured Linear client/auth. It now parses the document through a real AST and rejects malformed, operation-free, multi-operation, and subscription documents before transport. No Studio authorization policy is imposed on default upstream mode. | Accepted |
| STUDIO-0017 | R0-05 | B | Option-selected managed `DynamicTool` policy, AppServer/AgentRunner trusted-context wiring, `Tracker.ManagedMutation`, injected `Tracker.Outbox` port, and denial/outbox tests | Establish the narrow authorization and delegation seams needed for later deterministic Studio tracker lifecycle ownership. | Managed raw access is bounded, read-only, AST-allowlisted, and bound to trusted current issue/run context; mutations, subscriptions, fragments, directives, introspection, relationship fan-out, field arguments outside the exact issue binding, and unknown paths fail before Linear. Lifecycle commands use a strict immutable envelope and an out-of-band `source: :studio`; a missing sink fails closed and a supplied sink is invoked at most once with no automatic retry. AppServer and AgentRunner remain upstream by default; explicit `managed: true` selects the non-durable seam. Studio configuration remains R1-03 and persistence, delivery, reconciliation, and completion confirmation remain R1-01/R1-07. | Accepted |
| STUDIO-0018 | R0-05 | B | Package checkpoint, implementation journal, threat register, patch ledger, downstream behavior documentation, compatibility manifest, `scripts/codex_schema.py`, `scripts/test_codex_schema.py`, and the source-bound fixture gate | Record the R0-05 implementation boundary, security assumptions, deferrals, and acceptance evidence without overstating later release readiness; bind the new cancellation, cleanup, hook, runtime, and outbox surfaces into the exact compatibility replay. | Root `SPEC.md` remains the upstream authority and is not modified. The documentation distinguishes implemented in-process seams from the durable R1 tracker/claim contract, resolves runner independence and the one-trusted-operator pathname boundary, and records the 133-file/411-test source seal, 535-test complete gate, installed-pin verification, no-model smoke, hygiene audit, reproducible packaging, and independent exact-tree review while retaining truthful R0-06 and R1 deferrals. The fixture inventory retains all 18 prior fixture files in their original order and appends exactly `cancellation_lifecycle_test.exs`, `cancellation_safety_regression_test.exs`, `hook_cancellation_containment_test.exs`, `runtime_supervisor_test.exs`, and `tracker_outbox_test.exs`; it introduces no fixture-file removal or gate skip/exclusion. Intentional assertions rewritten inside retained files exercise behavior classified by STUDIO-0014–STUDIO-0017, STUDIO-0019, and STUDIO-0020. The exact count rises from 308 to 411. Its bounded child deadline rises from 180 to 300 seconds because the exact replay completes in 269.7 seconds, while the 1 MiB output bound, process-tree cleanup, immutable private snapshot, exact-summary oracle, source seal, and self-tests remain enforced. | Accepted |
| STUDIO-0019 | R0-05 | A | `Config.ManagedWorkspace`, `CleanupGuardian`, `WorkspaceHookRunner`, `RuntimeSupervisor`, `CleanupBarrier`, `Config.Schema`, `elixir/mix.exs`, and direct/adversarial coverage suites | Keep the 100% measured-module threshold honest after adding PID/monitor scheduling, OS-process cleanup, and same-UID pathname-race defenses that cannot be deterministically line-instrumented without production-only fault hooks. | The R0-03 structural-coverage policy is extended only to `CleanupGuardian`, `Config.ManagedWorkspace`, and `WorkspaceHookRunner`. Their behavior remains mandatory in cancellation-safety, hook-containment, workspace/config, and runtime-supervisor suites. `RuntimeSupervisor`, `CleanupBarrier`, and `Config.Schema` remain measured at 100.00%; the repository threshold stays 100%; no test, Dialyzer warning, or Credo finding is suppressed. | Accepted |
| STUDIO-0020 | R0-05 | A | Orchestrator typed workflow-validation failure classification and the hot-reload log regression | Keep unsupported downstream release configuration blocked before dispatch without misclassifying workflow validation as tracker transport failure or raw-inspecting the feature/release tuple. | Valid local workflow and genuine tracker-failure behavior remain unchanged. The completed-poll oracle requires the stable `failure_kind=unsupported_release_feature` line, rejects `Failed to fetch from tracker`, `remote_workers`, and `release_5`, retains blocked state, and proves no SSH dispatch. The exact failing seed, focused stress, source reseal, and complete repaired-tree gates pass. | Accepted |

Class C entries additionally require all of: triggering version, reason,
removal condition, regression test, owner, and upstream issue URL. A Class C
change cannot be accepted with any field omitted.

## Revision history

- Revision 1 — 2026-07-14: opened for the R0-01 metadata and provenance slice.
- Revision 2 — 2026-07-14: classified deterministic upstream test-harness hardening as Class A.
- Revision 3 — 2026-07-14: recorded Apache-2.0 per-file notices, target release, and stronger sanitizer oracle.
- Revision 4 — 2026-07-14: pinned Codex 0.144.3 and classified the generated compatibility contract as a Studio extension.
- Revision 5 — 2026-07-14: opened deterministic App Server/Linear fixtures and the network-hermetic test baseline for independent review.
- Revision 6 — 2026-07-14: bound fixture evidence to source, reconciled production request shapes, expanded the exact method/field matrix, and repaired deterministic supervised test ownership.
- Revision 7 — 2026-07-14: reclassified generic App Server conformance fixes as Class A and made fail-closed callback and exact sandbox behavior explicit.
- Revision 8 — 2026-07-15: accepted R0-02 after the source-bound seal, exact 67-test schema gate, deterministic regeneration, 266-test/100%-measured-coverage full gate, Dialyzer, and independent adversarial review all passed.
- Revision 9 — 2026-07-15: classified the R0-03 transport, PID-namespace containment, compatibility-circuit, remote-release-gate, vendored-erlexec, and source-bound conformance-evidence changes while final package gates remain in progress.
- Revision 10 — 2026-07-16: separated Class A remote/tooling hardening from the Class B durable compatibility circuit and diagnostics, then accepted R0-03 after the 113-file/276-test source seal, installed-pin and clean-regeneration checks, 73-test schema harness, 398-test/100%-measured-coverage full gate, zero-error Dialyzer analysis, real no-model App Server smoke, cleanup stress, and independent adversarial reviews all passed.
- Revision 11 — 2026-07-16: classified the R0-04 event/identity boundary, logical operation correlation, additive observability projection, and source-bound evidence updates while final package validation and independent re-review remain in progress.
- Revision 12 — 2026-07-16: accepted R0-04 after the 122-file/308-test source seal, installed-pin and clean-regeneration checks, 73-test schema harness, 432-test/100%-measured-coverage full gate, zero-error Dialyzer analysis, real initialize-only App Server smoke, hygiene audit, and independent adversarial reviews all passed.
- Revision 13 — 2026-07-16: classified the R0-05 cancellation/retry supervisor, exact-bound workspace and contained-hook hardening, root-compatible GraphQL parser fix, option-selected managed Linear/outbox seam, and documentation boundary while final-tree validation and independent review remain pending.
- Revision 14 — 2026-07-16: repaired lifecycle-hook cleanup propagation, supervised hook-task shutdown and retained cleanup authority, published exact bindings before hooks, restored default-upstream AgentRunner behavior, and resolved the trusted single-operator pathname boundary while final package gates remain pending.
- Revision 15 — 2026-07-16: classified the direct Connection supervision, durable ready-acknowledged cleanup handle, reciprocal CleanupBarrier/CleanupSupervisor restart barriers, startup-cleanup retention, and single-owner barrier-loss behavior while final package gates and independent review remain pending.
- Revision 16 — 2026-07-16: recorded the then-intermediate monotonic Connection-to-guardian handoff plus max-concurrency-one close/owner-death and barrier-registration regression surfaces while final package gates remained pending; Revision 17 replaces that intermediate physical-stop ownership.
- Revision 17 — 2026-07-16: superseded Connection-owned physical stopping with guardian-only `request_cleanup_once` delegation, registered Connection/AgentRunner/WorkspaceHookRunner lifetimes as reciprocal nested-supervisor barriers, bounded runtime restart intensity to 10 in 5 seconds, and recorded the nested-supervisor crash regressions while final package gates remain pending.
- Revision 18 — 2026-07-16: classified ambiguous Task.Supervisor guardian starts as non-transferable until a token-matched ready child is adopted, retained an indefinite fail-closed hold when no acknowledgement arrives, and recorded the child-start/supervisor-exit/starter-error maximum-concurrency-one regression while final package gates remain pending.
- Revision 19 — 2026-07-16: resolved the prior P2 no-acknowledgement oracle gap with an ambiguous-starter-without-child regression that remains held beyond the ready timeout, preserves the runtime generation, records zero adapter-stop calls and zero maximum concurrency, and never enters inline fallback; the focused runtime suite reports 12 tests with zero failures and the fixture-count oracle is 389, while final package gates remain pending.
- Revision 20 — 2026-07-16: completed the 133-file/406-test compatibility seal and final deterministic gates; extended the explicit structural line-instrumentation policy to `CleanupGuardian`, `Config.ManagedWorkspace`, and `WorkspaceHookRunner` while retaining direct adversarial tests and the 100% threshold; the complete 530-test gate, installed-pin verification, clean regeneration, and real initialize-only smoke pass, while hygiene/archive proof and final exact-diff review remain pending.
- Revision 21 — 2026-07-16: repaired final-review findings around infinite-shutdown cancellation, timeout capacity retention across tracker refresh, established-guardian verification publication, and terminal turn clearing; synchronized the supervisor-loss oracle on the old process's exact `:DOWN`; sealed 133 files/411 tests at source SHA-256 `9b6279e1c42f28153340248ca8f10b62321bb8bdf5930e988ea7384ae0afc3c0`; and passed the seed-42 replay plus 535-test/100%-measured-coverage/Dialyzer-zero full gate while hygiene/archive proof and final exact-diff review remain pending.
- Revision 22 — 2026-07-16: accepted R0-05 after provenance and secret/path/media/archive-exclusion hygiene passed, two normalized final-tree archives compared byte-for-byte, two fresh independent exact-tree reviews resolved the sole stale-header P2 and returned GO with no remaining P0/P1/P2, and an acceptance-only documentation recheck preserved every R0-06 and R1 deferral.
- Revision 23 — 2026-07-17: reopened the unpushed R0-05 acceptance after mandatory pre-push coverage seed `375551` exposed a typed workflow-validation result being mislabeled as a tracker-fetch failure and raw-inspected by Orchestrator; added the content-free unsupported-feature classification and deterministic hot-reload log oracle; explicitly classified the five-file fixture-inventory expansion, 308-to-411 exact-count change, and still-bounded 180-to-300-second child deadline; resealed 133 files/411 tests at source SHA-256 `24121b449990e852cf1b34ffad38d7aa4851aac2432bbeb19b052961264a52ae`; rejected host-global Codex drift to 0.144.5 and restarted validation with user-local exact 0.144.3; then passed focused repeat and seed replays, the 73-test schema harness, 535-test/100%-measured-coverage complete gate, zero-error Dialyzer analysis, installed-pin verification, no-model smoke, refreshed hygiene/reproducible packaging, and repaired-tree independent review with no remaining P0/P1/P2. This revision supersedes the unpushed Revision 22 acceptance tree without rewriting its historical evidence.
