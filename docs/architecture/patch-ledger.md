# Patch ledger

Ledger revision: `70`

This ledger classifies every non-trivial downstream change. Documentation-only
records may be grouped when they establish one coherent package boundary.

| ID | Package | Class | Surface | Purpose | Preservation / removal evidence | Status |
|---|---|---|---|---|---|---|
| STUDIO-0001 | R0-01 | B | Repository metadata and documentation | Lock target/provenance, route the two specifications, document fork policy, governance, preflight, and threat model. | No runtime behavior changes; root `SPEC.md`, `LICENSE`, and `NOTICE` hashes are recorded in R0-01 evidence. Modified upstream README carries a visible downstream notice. | Accepted |
| STUDIO-0002 | R0-01 | A | `elixir/test/symphony_elixir/core_test.exs`, `orchestrator_status_test.exs` | Make retry scheduling and dashboard semantics deterministic by using the memory tracker, an exact scheduling bracket, explicit render width, and a raw sanitizer oracle. | Test-only; production modules are unchanged and no upstream test is removed or disabled. Both files carry downstream notices. Narrow/wide focused tests, three retry seeds, and full upstream `make all` pass. | Accepted |
| STUDIO-0003 | R0-02 | B | `CODEX_VERSION`, `CODEX_LOCK.json`, generated schema bundle, method/field matrix, schema loader, and verifier | Pin the exact Codex 0.144.3 App Server contract and make stable/experimental protocol presence machine-verifiable without inventing runtime capabilities. | Raw Codex-generated files are retained. JSON uses a documented semantic hash because aggregate definition order is nondeterministic; TypeScript uses raw hashes. Clean regeneration, exact executable checksums, matrix assertions, and the package-local loader pass. | Accepted |
| STUDIO-0004 | R0-02 | A | Strict fake App Server, stateful loopback fake Linear, network-hermetic test startup, supervised test lifecycle, and `make schema` | Replace weak line-count/external-network/lifecycle assumptions with reusable ordered protocol fixtures, a real-client loopback boundary, and deterministic supervisor ownership. | Production tracker and orchestration behavior is unchanged. Ordinary tests cannot address the real Linear endpoint; the separately gated live E2E opts in explicitly. Strict method/ID/schema, mismatch, isolation, pagination, blocker, mutation, uncertainty, lifecycle stress, and full-suite gates pass. | Accepted |
| STUDIO-0005 | R0-02 | A | `Config.Schema`, Codex App Server startup requests, and dynamic-tool declarations | Reconcile production outbound requests, sandbox policy, callback decisions, and dynamic-tool responses with the pinned Codex 0.144.3 generated contract. | Initialization order and Symphony orchestration remain intact. Exact fixtures prove the request and response envelopes; legacy `reject` policy normalizes to granular booleans, unsupported policy shapes fail closed, `never` cannot approve callbacks, and only pinned sandbox variants reach the wire. | Accepted |
| STUDIO-0006 | R0-03 | A | `Codex.Connection`, `JSONLFramer`, `RequestPolicy`, `StderrDiagnostics`, `TransportError`, `AppServer`, configuration, and strict App Server fixtures | Replace line-buffered mixed transport assumptions with strict bounded JSONL, separate bounded stderr, exact request correlation, absolute deadlines, method-classified overload handling, and conservative uncertainty for side-effecting requests. | The pinned request shapes and original App Server workflow remain. Focused tests cover fragmentation/coalescing, messages beyond one MiB, the 16 MiB decoded-frame limit, malformed/contaminated stdout, duplicate/unexpected/late responses, initialization order, bounded `-32001` retry, terminal acknowledgement races, content-free prompt-safe diagnostics, and no blind side-effect retry. The final source-bound transport suite and complete upstream gate pass. | Accepted |
| STUDIO-0007 | R0-03 | A | `ProcessAdapter`, `CleanupGuardian`, `ErlexecRuntime`, application supervision, and vendored `erlexec` 2.3.4 | Execute the App Server argv without a shell and prove synchronous local containment across owner death, cancellation, timeout, hostile descendants, PID reuse, and cleanup-helper failure. | OpenAI Symphony remains the runner and the target command contract remains argv-based. The per-attempt containment launcher/target bootstrap starts empty, exact PID-namespace root/target identities are captured before target release, target environment is bounded and target-only, and cleanup retains independent pidfd authority over the namespace root and anchored outer group. The long-lived vendored `exec-port` remains an upstream trusted-runtime boundary; its build-only patches are documented. Deterministic, adversarial, 100-iteration cleanup replays and the complete gate pass. | Accepted |
| STUDIO-0008 | R0-03 | A | `AgentRunner`, `Orchestrator`, `SSH`, configuration, and the remote live-E2E gate | Fail closed after unverified cleanup and prevent unsupported remote-worker configuration or hot reload from reaching an SSH/process dispatch path. | Scheduler admission, retry, and tracker semantics remain upstream-compatible for valid local workflows. Remote execution is explicitly release-gated rather than partially implemented. Startup, reload, retry, stale-state, no-SSH, and deferred-remote-E2E tests pass. | Accepted |
| STUDIO-0009 | R0-03 | B | `CompatibilityCircuit`, AgentRunner/Orchestrator admission, safe status metadata, compatibility-manifest transport evidence, schema tooling, release documentation, third-party provenance, and threat register | Persist a source-identity-bound dispatch circuit after protocol incompatibility, bind the focused conformance result to the pinned Codex/schema/source seal without prematurely claiming R0-06 runtime capabilities, and record the local Linux/erlexec security boundary. | The compatibility marker is workspace scoped and mode `0600`; the full per-operation ledger and reconciliation reducer remain R1-07. `fixtures` and `transportConformance` are `pass`; `runtimeCapabilities` remains `not_run` and overall compatibility remains `pending_r0_06`. Strict validation rejects stale/mixed circuit or evidence states. The accepted seal binds 113 files and 276 tests to source SHA-256 `fc44e9692d6f443256ee304ae7ccd9df8e7267eaa1e5d453648956143eb47f91`; the complete 398-test upstream gate and independent reviews pass. | Accepted |
| STUDIO-0010 | R0-03 | A | Root `.gitattributes`, `elixir/Makefile`, and `elixir/mix.exs` quality-gate configuration | Keep the full gate executable across supported OTP warning forms, type-check the vendored runtime contract, preserve byte-exact third-party inputs, and make structural line-coverage boundaries explicit without suppressing analysis findings. | Dialyxir 1.4.7's crashing `short` warning formatter is replaced by Dialyzer's native formatter as the durable gate policy, not a version-scoped workaround; warning analysis and nonzero exit semantics remain unchanged. `:erlexec` is added to the PLT, no warning ignore is configured, the coverage threshold remains 100%, every R0-03 structural exclusion has direct/adversarial tests, and the complete gate reports zero Dialyzer errors and 100% of measured modules. Git whitespace diagnostics exempt only ten unchanged upstream erlexec files with byte-preserved legacy whitespace; downstream-authored and patched vendor files remain checked. | Accepted |
| STUDIO-0011 | R0-04 | A | `Identity`, canonical `Event`, injected `EventSink` with Noop/Memory adapters, AgentRunner/Orchestrator lifecycle propagation, Presenter additive correlation, and focused tests | Add stable run/attempt/event identity and an optional normalized event boundary without replacing Symphony's scheduler or requiring Studio persistence. | Noop retains nothing by default; Memory replay is bounded and process-local with per-run FIFO retention and deterministic run eviction by oldest successful new-event append. Existing callbacks and API fields remain intact, stale attempts cannot regress the live projection, sink history loss cannot block safety state, and durable authority remains R1-01/R1-02/R1-07. The 308-test source-bound seal, complete 432-test gate, and independent adversarial reviews pass. | Accepted |
| STUDIO-0012 | R0-04 | A | `Codex.Connection`, `Codex.AppServer`, logical operation correlation, and regression tests | Assign a UUIDv4 identity before each logical outbound operation and carry it through acknowledgement, bounded wire retry, failure, and uncertainty. | JSON-RPC request IDs and canonical request hashes retain their original roles; identical intentional calls remain distinct; uncertainty is attributed to the actual operation; private parameters are excluded; and the durable operation ledger remains R1-07. Retry/uncertainty regressions and a real initialize-only pinned-Codex smoke pass. | Accepted |
| STUDIO-0013 | R0-04 | B | Package checkpoint, journal, threat register, Build Week delta, behavior docs, and source-bound compatibility evidence/tooling | Record the Studio release boundary and bind the new event/idempotency contracts to deterministic evidence without claiming later durability or readiness. | The accepted manifest retains `runtimeCapabilities=not_run` and `overall=pending_r0_06`; its 122-file/308-test source seal, complete upstream gate, no-model smoke, hygiene checks, and independent reviews pass. | Accepted |
| STUDIO-0014 | R0-05 | A | `RuntimeSupervisor`, registered `Codex.CleanupBarrier`, `CleanupSupervisor`, `ConnectionSupervisor`, `Codex.CleanupGuardian`, `Codex.Connection`, `Codex.ProcessAdapter.StartupCleanup`, AgentRunner cancellation/authority retention, `Codex.AppServer` turn interruption, WorkspaceHookRunner containment, Orchestrator terminal/retry reconciliation, and lifecycle regressions | Make cancellation converge before hooks or workspace deletion, keep exactly one cleanup actor authoritative through ambiguous guardian startup, ordinary teardown races, nested-supervisor crashes, runtime child crashes, and startup rollback, prevent a restarted in-memory orchestrator from coexisting with predecessor connections, agents, or hook tasks, bind cleanup to the workspace that actually ran, and make retry removal timer-safe. | The upstream orchestrator remains scheduler authority. The `:one_for_all` runtime starts the cleanup registry, cleanup-task supervisor, and direct Connection supervisor before hook/agent supervisors and Orchestrator; restart intensity is bounded to 10 restarts in 5 seconds. A guardian acknowledges readiness only after trapping exits and monitoring its owner; its token-matched handle carries durable verification state and is registered with the cleanup barrier. Guardian start errors after invoking Task.Supervisor are treated as ambiguous because a child may already exist: the starter waits for that exact ready token, adopts the child if it acknowledges, and otherwise holds fail closed forever. Only positively observed non-start or known child absence can transfer authority toward caller-owned cleanup; an ambiguous result never falls through to inline cleanup. `CleanupBarrier` and `CleanupSupervisor` are reciprocal shutdown barriers if either one crashes. The barrier also registers and monitors every established Connection, AgentRunner, and WorkspaceHookRunner lifetime, so killing their nested Connection/Task/WorkspaceHook supervisor cannot permit a replacement runtime to coexist with a trapped predecessor member. Connection never performs an ordinary physical adapter stop: its first teardown request delegates to `CleanupGuardian.request_cleanup_once/2`, and only the guardian's bounded worker invokes `adapter.stop`; the established handle is published before readiness and marked durably verified before success is observable, while failure commits `cleanup_authority: :guardian`, and every later close, owner-down, supervisor-exit, child-exit, failure, or terminate path observes that handoff. Barrier-registration loss likewise keeps the acknowledged guardian as the sole actor while `CleanupSupervisor` survives; if both runtime authorities are unavailable, guardian cleanup must become durably verified before startup raises and any caller-owned inline fallback can run. AgentRunner and hook runners retain and monitor startup-cleanup handles, accept a late exit only when durable status proves cleanup, and otherwise hold the runtime closed. A cooperative stop requests `turn/interrupt`, retires the App Server connection, worker, hook runner, and hook descendant, runs `after_run` once, and returns the attempt-bound workspace. Cancellation failure or timeout never synchronously terminates the infinite-shutdown controller: a live retained PID remains safety-blocked and consumes global, immutable admitted-state, and worker-host capacity until exit. Terminal turn events clear stale interrupt targets. The exact binding is published before hooks; same-sender lifecycle events preserve ordering; unverified cleanup preserves the claim/workspace without retry. Terminal cleanup revalidates that exact path/root before `before_remove` and deletion, and every removed retry entry first cancels its timer. Durable claims, crash-window operation reconciliation, and repeated-failure circuit breaking remain R1-07/R1-12. | Accepted |
| STUDIO-0015 | R0-05 | A | `Workspace`, `WorkspaceHookRunner`, `PathSafety`, workflow/config root normalization, managed sandbox narrowing, remote-worker gates, behavior documentation, and path/hook tests | Preserve per-issue isolation while making identifier mapping, symlink handling, hook execution, sandbox roots, and cleanup fail closed at the local host boundary. | Ordinary safe identifiers and the upstream per-issue layout are retained; repaired or truncated identifiers receive a deterministic digest suffix. Relative roots bind to the selected `WORKFLOW.md` directory and are canonicalized once. Issue leaves must be direct non-symlink children; bound cleanup revalidates the captured canonical root around hooks. Local hooks receive an explicit environment, bounded content-free output accounting, supervised descendant cleanup, and retained cleanup authority; failed creation rolls back only a new workspace, never a reused one. The userspace deletion sequence is scoped to the supported one-trusted-operator, non-hostile-same-UID R0 profile. Upstream sandbox resolution remains available, opt-in managed mode binds writable roots to the exact issue workspace, and remote work remains gated to Release 5. | Accepted |
| STUDIO-0016 | R0-05 | A | `Codex.DynamicTool`, pinned Absinthe 1.11.0 parser dependency, and raw `linear_graphql` contract tests | Enforce the root Symphony contract that one raw Linear tool call contains exactly one query or mutation without replacing the existing high-trust tool. | Default upstream mode still accepts one raw query or mutation, including its original fragments and directives, and uses the configured Linear client/auth. It now parses the document through a real AST and rejects malformed, operation-free, multi-operation, and subscription documents before transport. No Studio authorization policy is imposed on default upstream mode. | Accepted |
| STUDIO-0017 | R0-05 | B | Option-selected managed `DynamicTool` policy, AppServer/AgentRunner trusted-context wiring, `Tracker.ManagedMutation`, injected `Tracker.Outbox` port, and denial/outbox tests | Establish the narrow authorization and delegation seams needed for later deterministic Studio tracker lifecycle ownership. | Managed raw access is bounded, read-only, AST-allowlisted, and bound to trusted current issue/run context; mutations, subscriptions, fragments, directives, introspection, relationship fan-out, field arguments outside the exact issue binding, and unknown paths fail before Linear. Lifecycle commands use a strict immutable envelope and an out-of-band `source: :studio`; a missing sink fails closed and a supplied sink is invoked at most once with no automatic retry. AppServer and AgentRunner remain upstream by default; explicit `managed: true` selects the non-durable seam. Studio configuration remains R1-03 and persistence, delivery, reconciliation, and completion confirmation remain R1-01/R1-07. | Accepted |
| STUDIO-0018 | R0-05 | B | Package checkpoint, implementation journal, threat register, patch ledger, downstream behavior documentation, compatibility manifest, `scripts/codex_schema.py`, `scripts/test_codex_schema.py`, and the source-bound fixture gate | Record the R0-05 implementation boundary, security assumptions, deferrals, and acceptance evidence without overstating later release readiness; bind the new cancellation, cleanup, hook, runtime, and outbox surfaces into the exact compatibility replay. | Root `SPEC.md` remains the upstream authority and is not modified. The documentation distinguishes implemented in-process seams from the durable R1 tracker/claim contract, resolves runner independence and the one-trusted-operator pathname boundary, and records the 133-file/411-test source seal, 535-test complete gate, installed-pin verification, no-model smoke, hygiene audit, reproducible packaging, and independent exact-tree review while retaining truthful R0-06 and R1 deferrals. The fixture inventory retains all 18 prior fixture files in their original order and appends exactly `cancellation_lifecycle_test.exs`, `cancellation_safety_regression_test.exs`, `hook_cancellation_containment_test.exs`, `runtime_supervisor_test.exs`, and `tracker_outbox_test.exs`; it introduces no fixture-file removal or gate skip/exclusion. Intentional assertions rewritten inside retained files exercise behavior classified by STUDIO-0014–STUDIO-0017, STUDIO-0019, and STUDIO-0020. The exact count rises from 308 to 411. Its bounded child deadline rises from 180 to 300 seconds because the exact replay completes in 269.7 seconds, while the 1 MiB output bound, process-tree cleanup, immutable private snapshot, exact-summary oracle, source seal, and self-tests remain enforced. | Accepted |
| STUDIO-0019 | R0-05 | A | `Config.ManagedWorkspace`, `CleanupGuardian`, `WorkspaceHookRunner`, `RuntimeSupervisor`, `CleanupBarrier`, `Config.Schema`, `elixir/mix.exs`, and direct/adversarial coverage suites | Keep the 100% measured-module threshold honest after adding PID/monitor scheduling, OS-process cleanup, and same-UID pathname-race defenses that cannot be deterministically line-instrumented without production-only fault hooks. | The R0-03 structural-coverage policy is extended only to `CleanupGuardian`, `Config.ManagedWorkspace`, and `WorkspaceHookRunner`. Their behavior remains mandatory in cancellation-safety, hook-containment, workspace/config, and runtime-supervisor suites. `RuntimeSupervisor`, `CleanupBarrier`, and `Config.Schema` remain measured at 100.00%; the repository threshold stays 100%; no test, Dialyzer warning, or Credo finding is suppressed. | Accepted |
| STUDIO-0020 | R0-05 | A | Orchestrator typed workflow-validation failure classification and the hot-reload log regression | Keep unsupported downstream release configuration blocked before dispatch without misclassifying workflow validation as tracker transport failure or raw-inspecting the feature/release tuple. | Valid local workflow and genuine tracker-failure behavior remain unchanged. The completed-poll oracle requires the stable `failure_kind=unsupported_release_feature` line, rejects `Failed to fetch from tracker`, `remote_workers`, and `release_5`, retains blocked state, and proves no SSH dispatch. The exact failing seed, focused stress, source reseal, and complete repaired-tree gates pass. | Accepted |
| STUDIO-0021 | R0-06 | A | `Codex.Connection`, `Codex.AppServer`, `Codex.RequestPolicy`, capability decoder/discovery/error modules, strict fake App Server and Responses fixtures, and protocol regressions | Add exact no-model capability discovery while preserving the initialized upstream runner and the generated 0.144.3 request contract. | Omitted JSON-RPC params remain distinct from `{}` through send/hash/retry/diagnostics; discovery initializes once, bounds pagination and output, emits content-free failures, and never starts a thread or turn. Ordinary runner/CLI behavior remains available. Targeted deterministic coverage is green; the repaired final source-bound and installed-pin replays remain pending. | Rework |
| STUDIO-0022 | R0-06 | B | Capability report, quota-shape normalization, protected identity binding, capability Mix task, and task-output tests | Produce the redacted Doctor-ready Studio reference-profile record without adding R1 policy, persistence, waits, or UI. | Model/effort/tier identifiers stay provider-supplied and opaque; sparse and full quota shapes stay distinct; raw email and credential material never enter reports; identity sameness fails closed; protected key inputs require a current-user-owned mode-`0700` parent and current-user-owned mode-`0600` regular key in both the runtime and readiness copier; one final task report is emitted only after exact receipts agree. A successful optional result retains `status=available` in the public capability report but normalizes its request-receipt outcome to `pass`; unavailable, unsupported, and auth-restricted reads retain their distinct outcomes. Revision 52 retains the green ownership-negative replay and restores strict Credo compliance; the final sealed gate remains pending. | Rework |
| STUDIO-0023 | R0-06 | B | `elixir/priv/hooks/studio_depth_guard.exs` and pinned MultiAgentV2 cap/hook conformance | Supply a permanent Studio defense-in-depth guard for direct-child delegation and report the exact Codex 0.144.3 native-depth limitation. This is not a temporary Class C workaround: Studio retains the guard even if a future pin adds native enforcement. | Raw V2 values `1/2/3` map to `0/1/2` optional non-root children. The guard permits a root spawn and rejects a child's recursive spawn when executed. A separate regression proves hook execution failure is fail-open; the final manifest must report `nativeDepthEnforcement=false`, `studioDepthGuardRequired=true`, and the fail-open classification. R1-05 still owns the independent admission/interruption layer. Final exact-tree publication remains pending. | Rework |
| STUDIO-0024 | R0-06 | A | Canonical bounded Linear client, Adapter, capability discovery/task, broker client, Orchestrator error boundary, fake Linear transport, and regressions | Prevent credential exfiltration, private-response leakage, and partial GraphQL data from clearing blockers, admitting dispatch, or claiming a mutation succeeded. | Only the two canonical Linear endpoint spellings may receive authorization. Ordinary candidate pages, ID refreshes, viewer resolution, dispatch revalidation, comment creation, state lookup, and state update reject any top-level `errors` member before trusting data and return stable categories. Raw `Client.graphql/3` retains its upstream envelope-preserving contract. Deterministic/schema-only discovery never mutates Linear. The sealed task maps only six exact queries to bounded operation enums and uses one packet-four Unix session without a raw key or network. An external trusted supervisor retains the raw provider frames and admits only the exact supervisor-launched BEAM after lineage, executable-byte, code-root-byte, command, environment, namespace, route, and socket attestation. The 34-test affected slice and the protected Revision 60 query-only replay are green with exactly nine queries and zero mutations; final frozen-tree publication and fresh review remain pending. | Rework |
| STUDIO-0025 | R0-06 | B | `scripts/studio_readiness.py`, readiness compiler self-tests, source-bound command inventory, isolated quality/archive rehearsal, and recoverable schema/readiness/index publication | Bind Doctor-ready evidence to the exact staged source, Git semantics, installed tools, schema, compatibility matrix, platform, Linear result, and every Release 0 conformance outcome without trusting host build state. | The generic, build, and network-enabled staged-publisher sandboxes run non-root with no capabilities, private Git/HOME/XDG/dependency/Hex/Mix/build/coverage/escript/archive state, masked host temp/tool roots, private `/var/tmp`, private `/dev` and `/dev/shm`, finite writable mounts, command/status truth tables, bounded output, semantic Git fingerprints, and reproducible archive checks. They are structurally credential-blind: no Codex auth, identity key, Linear key, pointer selector, raw provider body, or credential-bearing file descriptor/path enters them. All six preparation phases are credential-free. After bootstrap, the staged publisher delegates over one owner-only fixed Unix control boundary to an external trusted supervisor. Protocol v4 carries only protocol, operation, and the original tree in its seal request, and returns only that tree plus an opaque transient seal ID. The publisher must carry the gate's originally captured index tree rather than recapturing it at contact; the supervisor independently pins its startup tree before publisher launch and rejects any mismatch before private reconstruction. Before any credential preparation, key-content read, provider access, or child launch, the supervisor reconstructs that exact Git-index workspace, independently builds it with only locked acquisition networked, retires the builder process group, and descriptor-projects all sorted application `ebin` roots plus the native `erlexec` port tree into unique publisher-invisible storage. Later requests carry only the seal ID plus public source/tool/installed-Codex bindings from the pinned publisher process. Only the Codex child receives a disposable writable copy of the minimum auth/identity inputs; only the networkless Linear child receives the fixed raw-response broker socket. Public responses are strict allowlisted projections. The readiness projection excludes hidden reference rows from public `models`, retains the complete `referenceProfile`, and requires its booleans and status to equal the visible model/effort, ChatGPT-authentication, and protected-identity conjunction. Publication keeps a secret-free Git environment, helper suppression, conventional index-lock fence, transaction-owned verifier index, durable attempt record, exact journals/backups through final verification, pre-sort archive bounds, and dependency PLT/hash filename-version consistency. Prepublication source/schema/installed-Codex verification is a dedicated source-bound command that never consults the superseded runtime pair. Atomic publication must converge the schema manifest, readiness artifact, and index to the original or one verified green candidate. The compiler, publisher before and under its transaction lock, and final canonical verifier independently require the exact 13 required rows all passing, empty blockers, passing runtime/platform/package status, and passing paired-schema runtime/overall status. A blocked pair remains diagnostic only. Exit-zero Dialyxir, green-but-unsafe publisher, deterministic-blocked, raw-key publisher, secret-visible publisher, shared-runtime tamper/restore, and transient-index substitution candidates are rejected evidence. The repaired readiness suite passes 57/57, the external adversarial supervisor suite passes 47/47, and Revision 64 targeted acceptance tests pass with exact discovery count 157. The Revision 65 complete attempt preserved the semantic Git-metadata fence and passed every earlier row, but its final `upstream_make_all` child exhausted the audited 1,800-second deadline while parallel host workloads were active. A credential-free diagnostic reconstructed the exact staged snapshot and private sandbox, passed that row in 1,248.048 seconds with 660 tests, 100% measured coverage, clean Credo, zero Dialyzer errors, no error marker, complete version-consistent PLTs, and a 15.422-second byte-stable offline replay, and passed the remaining output/integrity oracles. Revision 66 adds no runtime code or bound change and requires a host-resource-exclusive window for the one resealed authoritative retry. | Rework |
| STUDIO-0026 | R0-06 | A | `elixir/mix.exs` structural coverage inventory and direct/adversarial capability/task suites | Keep the 100% measured-module threshold honest for six OS/process/live-bound wrappers without suppressing their behavior. | `ReadOnlyBroker` joins `Codex.CapabilityDiscovery`, `Codex.IdentityBinding`, `Linear.CapabilityDiscovery`, `Mix.Tasks.Studio.Capabilities`, and `Mix.Tasks.Studio.LinearCapabilities` in the established structural line-instrumentation exclusions. Its direct packet-four Unix transport tests and the external broker adversarial suite remain mandatory; all tests, Dialyzer warnings, and Credo findings stay enabled. Revision 57 expects a 549-test exact source-bound replay and a fresh complete quality gate. | Rework |
| STUDIO-0027 | R0-06 | B | R0-06 checkpoint, implementation journal, threat model, patch ledger, readiness/schema pair, and source-bound fixture expansion | Record the exact no-guess boundary, rejected false-green evidence, live Linear evidence boundary/history, security assumptions, and later-release deferrals without presenting pre-seal observations as accepted release evidence. | Root `SPEC.md` remains byte-for-byte unchanged. Documentation separates deterministic/schema-only evidence from live capability, keeps R0-07 and R1 work pending, and requires any later source/doc edit to invalidate and republish the pair. It records Revision 59's independent-review rejection, Revision 60's external supervisor boundary, the command-local selector handoff required because parent tool environments filtered it, and the fact that neither the selector nor any secret/path/raw response enters public evidence. Revision 64 additionally records that Revision 63 staged and reported success for an internally consistent blocked pair, both fresh reviewers rejected that tree with the same P1, and blocked evidence can never satisfy publication acceptance. Revision 65 records the later semantic metadata-fence rejection without claiming a narrower unproved cause. Revision 66 records the subsequent final-row deadline under parallel host load and the exact isolated-row/PLT/replay diagnostic pass, and requires a host-resource-exclusive publication interval. The compatibility fixture retains all 23 R0-05 files in original relative order and adds exactly 15 named R0-06 files; `linear_read_only_broker_test.exs` raises the exact oracle to 549 without removing or disabling a test. The bounded child deadline remains 600 seconds with the 1 MiB output cap, private snapshot, descendant cleanup, and exact-summary rules unchanged. Fresh seal, complete harness, publication, archive/hygiene receipts, and exact-tree reviews remain pending. | Rework |
| STUDIO-0028 | R0-06 | B | Downstream `elixir/WORKFLOW.md` Linear binding, validation-fixture capability probe, redacted readiness evidence, and package records | Replace the pinned upstream workflow's inaccessible OpenAI-specific project binding with the fork owner's dedicated downstream validation project while preserving Linear as tracker of record and the upstream workflow contract. | Only the downstream project slug and existing generic lifecycle-state names enter public history. `Backlog` remains ineligible, and the exact terminal set continues to include `Closed`, both `Cancelled` and `Canceled`, `Duplicate`, and `Done`. The modified upstream workflow now carries a prominent comment-form downstream change notice inside its preserved YAML front matter, as required by Apache-2.0 section 4(b) and the fork policy. An opt-in release-validation mode proves two bounded Backlog fixture shapes, an actual comment, and an intra-pair blocker relation while retaining the ordinary non-fixture capability task for arbitrary valid projects. The default live path starts only Req, nested reads are bounded to 16, and a URL-style selector is accepted only when the returned `slugId` is identical or its exact hyphen-delimited suffix. The pre-freeze redacted probe verified project/team visibility, configured states and labels, both fixture shapes, a comment, a blocker, and an unchanged repeated snapshot. Team, issue, account, workspace, credential, comment, and relation identities remain process-local and redacted; every live operation is a query, mutation evidence remains schema-only, and no Linear mutation is executed. The repaired final source-bound replay and explicit no-mutation confirmation remain pending. | Rework |
| STUDIO-0029 | R0-06 | A | Authoritative private build fingerprint and compiler-link regressions | Accept normal pinned Mix/Rebar build structure without allowing a build link to escape, substitute, or weaken the source/dependency/build binding. | The inventory walker never follows links. Only exact compiler-owned Mix app-source, Phoenix colocated, and internal Rebar profile-plugin path forms are admitted beneath an exact Mix-environment prefix. Raw link text must equal the compiler-derived target in the sealed namespace, while target identity and presence are validated against and digest-bound to the separately fingerprinted host-owned private roots. Only the exact Phoenix `assets/node_modules` target may be missing. Dependency/source/tool inventories remain blanket symlink-rejecting. Absolute Mix, external/wrong/missing/file/symlink-component, arbitrary, oversized, and crafted second-link-plus-`..` substitutions fail closed. Revision 52 retains complete build-byte/link validation after credential-free compilation, around both authenticated tasks, between every exact gate, and at final verification. Positive/adversarial link cases pass 2/2 and the real private-sandbox regression passes 1/1 in 229.836 seconds; the authoritative publisher and repaired-tree review remain pending. | Rework |
| STUDIO-0030 | R0-06 | A | Standalone authenticated Codex entry, credential-free brokered Linear entry, and trusted external capability supervisor | Prevent candidate-controlled project/dependency loaders, aliases, compilers, scripts, shell-backed metadata, arbitrary provider requests, or a networked staged publisher from acquiring release credentials or raw provider responses. | The ordinary `mix studio.capabilities` and `mix studio.linear_capabilities` commands remain independently usable. Protocol v4 seal requests carry only protocol, operation, and the publisher's originally captured index tree; responses carry only that tree and an opaque transient seal ID. The external supervisor independently pins the startup tree before publisher launch, reconstructs and builds it from verified Git objects with only locked acquisition networked, retires the builder process group, and descriptor-projects every sorted `ebin` root plus exact native-port bytes/modes into unique publisher-invisible storage before any credential access. Later requests carry the seal ID and public source/tool/installed-Codex bindings from the exact pinned publisher process. The Codex child alone receives a disposable writable copy of validated auth/identity files plus the verified native executable. The Linear child receives no raw key or direct network; its `-pa` paths and expected peer fingerprints both derive from the private runtime, and the supervisor-side broker retains every raw response and admits only that exact launched BEAM. Neither entry invokes Mix, mise, an alias, compiler, runtime config, Rebar script, or project loader. Direct-child output, deadlines, process groups, public projections, credentials, private-seal cleanup, and request cleanup are bounded and fail closed. The readiness suite passes 57/57, the post-audit affected slice passes 4/4, and the external adversarial suite passes 47/47. Revision 63 protected query-only and no-model replays remain valid targeted evidence; its documentation-frozen blocked pair and reviews are rejected. Revision 64 repaired acceptance semantics; its final publisher failed closed on the metadata fence. Revision 65 preserved that fence but exhausted the final make-all deadline under parallel host workload; the exact isolated row and every PLT/replay postcheck then passed. Revision 66 host-exclusive authoritative publication and fresh reviews remain pending. | Rework |
| STUDIO-0031 | R0-06 | A | Capability-pagination typing, Linear decoder totality, private complete-gate timeout separation, and direct regressions | Preserve every unsuppressed upstream quality finding and finite safety bound while allowing the complete exact-tree gate enough time to return a real result. | The second atomic pair passed the repaired 143-test schema harness and every required row except `upstream_make_all`. A solitary exact-sandbox diagnostic returned exit 2 after 1,257.930 seconds: all 143 Python tests passed in 563.888 seconds, then Dialyzer reported five real warnings. The prior repair retains the existing page/item limits and duplicate-item sets, uses lists only for already-bounded cursor-cycle membership, expresses the typed tracker-kind check as a map-pattern match, consolidates the shared nonempty-state validator, and removes only unreachable client fallbacks whose existing downstream paths preserve malformed-response errors. Direct Dialyzer and the complete affected 126-test capability/client slice were green on that predecessor tree. The full `make all` deadline remains a distinct finite 1,800 seconds, while the already-built offline Dialyzer byte-stability replay remains independently bounded at 1,200 seconds; regressions bind both values and no warning or test is suppressed. Revision 42 records the predecessor-tree 13-command inventory and invalidates its seal. Revision 44's final-acceptance claim is rejected by Revision 45's independent findings; the current 51-test readiness and 31-test Linear slices are green, but the repaired 147-test schema harness, full `make all`, complete PLT/hash and offline-Dialyzer proof, atomic publication, and exact-tree reviews remain pending. | Rework |
| STUDIO-0032 | R0-06 | A | Retained `cancellation_safety_regression_test.exs` full-suite scheduling oracle | Keep the exact source-bound fixture reliable under demonstrated whole-suite containment teardown without weakening a production timeout or reopening accepted R0-05 behavior. | The first 546-count seal missed the dead-unverified-cleanup test's positive event after 5 seconds; because that case intentionally holds the runtime fail closed, later startup timeouts were cascading. The exact case passes 1/0 and the complete file passes 22/0 at seed 0 in 54.8 seconds. Only the positive `process_cleanup_failed` receive rises to 15 seconds. Production cleanup, cancellation, App Server, and aggregate 600-second fixture deadlines are unchanged. The repaired pre-freeze 546-test seal is green; the final documentation tree still requires exact resealing. | Rework |
| STUDIO-0033 | R0-06 | A | Final sealed application compile source selection | Require the credential-free build to compile the exact staged application rather than returning success over the dependency-only setup source. | The first two protected Revision 45 Linear attempts stopped before credential installation because the required application runtime root contained no BEAM files; neither made a provider request or mutation. The five dependency phases retain the bounded minimal setup overlay, while the sixth offline warnings-as-errors application compile uses the immutable staged snapshot and the same private writable dependency/build roots. A regression binds the exact five-true/one-false source-overlay sequence, and runtime-root validation independently rejects an empty required application `ebin`. The corrected protected replay passed every redacted project/team/state/label/fixture/comment/blocker/stability check with schema-only mutation evidence; every provider operation was a query and no mutation occurred. The complete repaired readiness suite passes 51/51 in 237.785 seconds; the final exact-tree seal, publisher, and reviews remain pending. | Rework |
| STUDIO-0034 | R0-06 | A | Network-hermetic nested snapshot fixture inside the private full gate | Preserve the real snapshot-fixture self-test without allowing a network-disabled generic gate to contact Hex, write an immutable outer cache, or silently skip dependency/NIF validation. | The first Revision 46 atomic pair truthfully blocked `schema_harness` and `upstream_make_all` while 11 other rows passed. Direct schema replay passed 147/147 in 811.807 seconds; exact private-sandbox reproduction failed after 59.340 seconds because the nested real fixture discarded the outer verified Hex cache and Mix tooling, then attempted `builds.hex.pm`. Standalone seals retain their existing network bootstrap. The first outer-only repair inherited the validated `HEX_HOME`, `MIX_ARCHIVES`, executable `MIX_REBAR3`, and `HEX_OFFLINE=1`; it removed the network attempt, but the next exact private replay failed in 59.694 seconds with `:eaccess` because offline Hex writes registry state while the outer cache is read-only. A bounded no-follow writable Hex-cache clone repaired that boundary; Mix archives and Rebar stay read-only. The following replay passed resolution but failed in 160.517 seconds because the nested XDG cache lacked the checksum-bound `lazy_html` precompiled NIF archive downloaded by the credential-free outer bootstrap. The final boundary additionally clones only a bounded flat inventory of regular `.tar.gz` NIF archives into the nested private cache; the offline dependency compiler retains its pinned-checksum validation. Entry and byte bounds are enforced before sorting or copying; missing, malformed, symlinked, incorrectly typed, changed, over-bound, or non-executable inputs fail closed. The existing environment oracle asserts standalone and outer-offline modes, both cache copies' metadata/content, unsafe-entry denial, and pre-materialization bounds. Fresh exact-sandbox replay, seal, publisher, and review evidence remain pending. | Rework |
| STUDIO-0035 | R0-06 | A | Sealed Linear capability task report collection | Preserve the exact query-only task behavior while satisfying the unsuppressed upstream strict-Credo gate. | The authoritative Revision 47 publisher passed 12 conformance rows and blocked `upstream_make_all` at Credo before Dialyzer because `run_with_probe/4` nested report collection three levels deep. Revision 48 moves only that collection into one private helper; argument parsing, workflow override/restoration, log suppression, probe invocation, canonical JSON, credential boundary, and single-record output are unchanged. All seven task tests pass, and strict Credo checks 123 source files and 3,886 modules/functions with no issue. Fresh source seal, complete publisher/PLT proof, and reviews remain pending. | Rework |
| STUDIO-0036 | R0-06 | A | Trusted out-of-tree Linear broker contract, staged Unix broker client, publisher/socket isolation, and adversarial tests | Make the R0-06 query-only claim technically true even if staged publisher or BEAM code is buggy or candidate-controlled. | Reviewer A rejected Revision 54 because the trusted launcher injected the raw key into staged Python/BEAM processes with direct network access. Revision 55 moved the key into a non-dumpable out-of-tree supervisor and introduced peer-attested broker isolation. Revision 56 additionally masks the protected credential's whole parent, keeps the exact pathname out of child arguments, and remounts only a private verified Codex-auth copy for the credential-blind publisher. Credential-free dependency bootstrap retains required network; only the exact sealed BEAM peer receives the mode-`0600` Unix socket in a distinct route-free network namespace beneath a mode-`0700` parent. Frames contain no query, URL, method, headers, auth, or operation name; the trusted broker maps six operation enums to fixed queries, binds public project/team/fixtures and learned team identity, enforces the exact nine-query replay plus non-provider finish, pins TLS host/port/path, ignores proxy configuration, rejects redirects/compression/oversize/duplicate keys/errors, and records a chained zero-mutation receipt. The external five-test adversarial suite, four staged Python boundary tests, and 34-test Elixir slice pass. The external executable remains outside Git by design; its exact hash/mode and final receipt must be recorded in the owner-only runtime checkpoint and independently reviewed before acceptance. | Rework |
| STUDIO-0037 | R0-07 | A | `SymphonyElixir.ReleaseInfo`, CLI `--version`, and focused tests | Give the independently installable Release 0 runner an exact public version and build-provenance command without starting the runtime or weakening its existing acknowledgement boundary. | Ordinary workflow startup and guardrail behavior are unchanged. Only the exact standalone `--version` form bypasses runtime startup; mixed runtime/version arguments fail. Post-merge packaging supplies the semantic version, merged commit, upstream base, Codex compatibility hash, and provenance as public compile-time values, and clean-install verification requires byte-exact output. | In progress |
| STUDIO-0038 | R0-07 | B | Publication Doctor, candidate/package/final manifests, deterministic package tooling, protected workflows, release docs, and recovery proof | Publish the accepted Release 0 foundation through a fail-closed, independently attested, protected release train. | Candidate workflows are read-only and receive no publication or trusted-attestor credential; the standard ephemeral read-only GitHub token may still be present, and general GitHub Actions App ID `15368` cannot satisfy the dedicated-attestor requirement. Candidate manifests remain outside the checkout to avoid commit self-reference and bind exact head/base/tree/workflow/evidence hashes. Protected-main publication builds only from the exact merge with version-pinned Hex, checksum-pinned Rebar, a fixed exclusive build root, two independent clean compilations, exact hash-gated dependency determinism patches, and a lock-bound runtime closure/SBOM/license-notice inventory. Source/ZIP/tar bounds precede allocation; the installed runner must load the exact source-bound Codex manifest, matrix, representative schema, compatibility identity, and build digest, then return a deterministic HTTP 200 zero-work projection as an unprivileged Debian user and leave no process group. Progressive atomic publication/failure receipts are retained through final verification; typed publication fields resolve through the exact custom-attestation predicate rather than a mutable asset. Retry may use only a protected-main descendant with the candidate-approved workflow hash while package bytes stay bound to the original merge. The latest dedicated-App CheckRun carries the exact canonical manifest. The App private key remains out of repository Actions, and its minimum installation has only Metadata/Contents/Actions/Pull requests read plus Checks write. Post-merge failures emit bounded action-required evidence; any active or merged manual release revert blocks publication, and automation never creates, closes, deletes, or merges it. The complete draft is downloaded and rehashed before immutable publication, exact tag/provenance/custom-predicate verification, and branch cleanup. Direct main pushes, protection weakening, tag reuse/movement, stale evidence, secret-shaped build environment, unsafe archive entries, incomplete notices/checksums, and mismatched assets fail closed. | In progress |
| STUDIO-0039 | Build Week Preview | B | Canonical Intent Service, bounded planner/store, local STDIO MCP server and Intent skill, protected Linear write broker, and admission bridge | Deliver the emergency-preview prompt/Markdown-to-reviewed-plan journey without letting a user-facing Codex thread bypass Symphony scheduling or letting the R0 read-only credential acquire write scope. | The preview explicitly pulls a narrow planning/publication slice forward from Release 2 under the deadline override and does not claim it as stable Release 1. The browser and MCP surfaces share one Intent Service, but MCP exposes only non-mutating attach, submit, clarify, present, and status operations; the trusted local UI alone owns approval, publication, and start. The broker permits only issue creation in Backlog, dependency creation, and one selected first-ready transition to Todo, rejects `SYM-1`/`SYM-2`, fails closed on uncertainty, and requires protected constant-time proof that its credential differs from R0. Normal Symphony polling/admission remains the only execution entry. No live Linear mutation or fixture change is claimed on the current candidate. | In progress |
| STUDIO-0040 | Build Week Preview | B | Phoenix LiveView New Work, Setup, Mission Control, Run Detail, authoritative event projection, and GET-only verification endpoint | Give an owner and judge one production-route control surface that reports real intent, dependency, run, validation, review, evidence, delivery, and tracker-handoff truth. | The interface consumes the canonical Intent Store plus runtime EventSink rather than a fixture-only success route. Completion reduces only the latest admitted attempt, requires complete ordered replay and explicit success, rejects unresolved review findings, separates requested from actual model/effort, and binds checks, review, current sealed evidence, immutable delivery, handoff, and terminal state to one source revision; incomplete or mixed proof stays incomplete. The server is loopback-only, the verification route is read-only, and usage is absent unless genuinely reported. Desktop/mobile Playwright, keyboard focus, Axe, and browser-error checks exercise the rendered routes. | In progress |
| STUDIO-0041 | Build Week Preview | B | Owner CLI, exact local reset, browser contract/golden-path tests, public audit, clean-launch gate, judge docs, and preview honesty boundary | Make the accelerated public branch installable and testable without applying Release 0's repeated acceptance ceremony to every preview edit. | Launch rebuilds the production runner unless an explicit safe no-build path is selected. Reset binds one exact persisted intent and non-fixture issue, bounds inventory before allocation/sort, performs only local filesystem quarantine, and records zero Linear mutations. The golden contract asserts only production probe fields, writes a handoff after authoritative completion, and covers real production state vocabulary plus an explicit unavailable route without synthetic success. Public audit rejects credentials, protected paths/material, and new submission media. The prerelease documents current read-only capability and retains the separate-credential, live Linear/Codex run, final review, and provenance work as blockers rather than presenting them as completed. | In progress |

Class C entries additionally require all of: triggering version, reason,
removal condition, regression test, owner, and upstream issue URL. A Class C
change cannot be accepted with any field omitted.

## Revision history

- Revision 1 — 2026-07-14: opened for the R0-01 metadata and provenance slice.
- Revision 2 — 2026-07-14: classified deterministic upstream test-harness hardening as Class A.
- Revision 3 — 2026-07-14: recorded Apache-2.0 per-file notices, target release, and stronger sanitizer oracle.
- Revision 4 — 2026-07-14: pinned Codex 0.144.3 and classified the generated compatibility contract as a Studio extension.
- Revision 5 — 2026-07-14: opened deterministic App Server/Linear fixtures and the network-hermetic test baseline for independent review.
- Revision 6 — 2026-07-14: bound fixture evidence to source, reconciled production request shapes, expanded the exact method/field matrix, and repaired deterministic supervised test ownership.
- Revision 7 — 2026-07-14: reclassified generic App Server conformance fixes as Class A and made fail-closed callback and exact sandbox behavior explicit.
- Revision 8 — 2026-07-15: accepted R0-02 after the source-bound seal, exact 67-test schema gate, deterministic regeneration, 266-test/100%-measured-coverage full gate, Dialyzer, and independent adversarial review all passed.
- Revision 9 — 2026-07-15: classified the R0-03 transport, PID-namespace containment, compatibility-circuit, remote-release-gate, vendored-erlexec, and source-bound conformance-evidence changes while final package gates remain in progress.
- Revision 10 — 2026-07-16: separated Class A remote/tooling hardening from the Class B durable compatibility circuit and diagnostics, then accepted R0-03 after the 113-file/276-test source seal, installed-pin and clean-regeneration checks, 73-test schema harness, 398-test/100%-measured-coverage full gate, zero-error Dialyzer analysis, real no-model App Server smoke, cleanup stress, and independent adversarial reviews all passed.
- Revision 11 — 2026-07-16: classified the R0-04 event/identity boundary, logical operation correlation, additive observability projection, and source-bound evidence updates while final package validation and independent re-review remain in progress.
- Revision 12 — 2026-07-16: accepted R0-04 after the 122-file/308-test source seal, installed-pin and clean-regeneration checks, 73-test schema harness, 432-test/100%-measured-coverage full gate, zero-error Dialyzer analysis, real initialize-only App Server smoke, hygiene audit, and independent adversarial reviews all passed.
- Revision 13 — 2026-07-16: classified the R0-05 cancellation/retry supervisor, exact-bound workspace and contained-hook hardening, root-compatible GraphQL parser fix, option-selected managed Linear/outbox seam, and documentation boundary while final-tree validation and independent review remain pending.
- Revision 14 — 2026-07-16: repaired lifecycle-hook cleanup propagation, supervised hook-task shutdown and retained cleanup authority, published exact bindings before hooks, restored default-upstream AgentRunner behavior, and resolved the trusted single-operator pathname boundary while final package gates remain pending.
- Revision 15 — 2026-07-16: classified the direct Connection supervision, durable ready-acknowledged cleanup handle, reciprocal CleanupBarrier/CleanupSupervisor restart barriers, startup-cleanup retention, and single-owner barrier-loss behavior while final package gates and independent review remain pending.
- Revision 16 — 2026-07-16: recorded the then-intermediate monotonic Connection-to-guardian handoff plus max-concurrency-one close/owner-death and barrier-registration regression surfaces while final package gates remained pending; Revision 17 replaces that intermediate physical-stop ownership.
- Revision 17 — 2026-07-16: superseded Connection-owned physical stopping with guardian-only `request_cleanup_once` delegation, registered Connection/AgentRunner/WorkspaceHookRunner lifetimes as reciprocal nested-supervisor barriers, bounded runtime restart intensity to 10 in 5 seconds, and recorded the nested-supervisor crash regressions while final package gates remain pending.
- Revision 18 — 2026-07-16: classified ambiguous Task.Supervisor guardian starts as non-transferable until a token-matched ready child is adopted, retained an indefinite fail-closed hold when no acknowledgement arrives, and recorded the child-start/supervisor-exit/starter-error maximum-concurrency-one regression while final package gates remain pending.
- Revision 19 — 2026-07-16: resolved the prior P2 no-acknowledgement oracle gap with an ambiguous-starter-without-child regression that remains held beyond the ready timeout, preserves the runtime generation, records zero adapter-stop calls and zero maximum concurrency, and never enters inline fallback; the focused runtime suite reports 12 tests with zero failures and the fixture-count oracle is 389, while final package gates remain pending.
- Revision 20 — 2026-07-16: completed the 133-file/406-test compatibility seal and final deterministic gates; extended the explicit structural line-instrumentation policy to `CleanupGuardian`, `Config.ManagedWorkspace`, and `WorkspaceHookRunner` while retaining direct adversarial tests and the 100% threshold; the complete 530-test gate, installed-pin verification, clean regeneration, and real initialize-only smoke pass, while hygiene/archive proof and final exact-diff review remain pending.
- Revision 21 — 2026-07-16: repaired final-review findings around infinite-shutdown cancellation, timeout capacity retention across tracker refresh, established-guardian verification publication, and terminal turn clearing; synchronized the supervisor-loss oracle on the old process's exact `:DOWN`; sealed 133 files/411 tests at source SHA-256 `9b6279e1c42f28153340248ca8f10b62321bb8bdf5930e988ea7384ae0afc3c0`; and passed the seed-42 replay plus 535-test/100%-measured-coverage/Dialyzer-zero full gate while hygiene/archive proof and final exact-diff review remain pending.
- Revision 22 — 2026-07-16: accepted R0-05 after provenance and secret/path/media/archive-exclusion hygiene passed, two normalized final-tree archives compared byte-for-byte, two fresh independent exact-tree reviews resolved the sole stale-header P2 and returned GO with no remaining P0/P1/P2, and an acceptance-only documentation recheck preserved every R0-06 and R1 deferral.
- Revision 23 — 2026-07-17: reopened the unpushed R0-05 acceptance after mandatory pre-push coverage seed `375551` exposed a typed workflow-validation result being mislabeled as a tracker-fetch failure and raw-inspected by Orchestrator; added the content-free unsupported-feature classification and deterministic hot-reload log oracle; explicitly classified the five-file fixture-inventory expansion, 308-to-411 exact-count change, and still-bounded 180-to-300-second child deadline; resealed 133 files/411 tests at source SHA-256 `24121b449990e852cf1b34ffad38d7aa4851aac2432bbeb19b052961264a52ae`; rejected host-global Codex drift to 0.144.5 and restarted validation with user-local exact 0.144.3; then passed focused repeat and seed replays, the 73-test schema harness, 535-test/100%-measured-coverage complete gate, zero-error Dialyzer analysis, installed-pin verification, no-model smoke, refreshed hygiene/reproducible packaging, and repaired-tree independent review with no remaining P0/P1/P2. This revision supersedes the unpushed Revision 22 acceptance tree without rewriting its historical evidence.
- Revision 24 — 2026-07-17: opened R0-06 capability discovery, reference-profile reporting, permanent Studio depth guard, bounded Linear discovery/error boundary, isolated readiness compiler, private-PLT repair, structural coverage policy, atomic evidence publication, and documentation surfaces. Deterministic capability and Linear suites plus the current 643-test/100%-measured-coverage non-seal gate pass, while the final staged source seal, repaired full gate, archive/hygiene proof, exact-tree reviews, and live Linear result remain pending. The depth guard is deliberately Class B defense-in-depth rather than an incomplete Class C workaround.
- Revision 25 — 2026-07-18: rejected the first green atomic publisher after adversarial review proved ambient fsmonitor execution and secret inheritance, a post-verifier writer false-success window, and index-mode narrowing. Repaired the boundary with a minimal Git environment and explicit helper suppression, a post-verifier publication fence with durable rollback and ambiguous-state preservation, and exact owner/group/mode/link metadata validation and preservation. The then-current 21-test focused transaction/integration slice and repair rereview passed; Revision 26 supersedes that implementation and its GO after authoritative execution exposed further gate and fence failures.
- Revision 26 — 2026-07-18: rejected the first authoritative publisher run after deterministic gate failures and stat-cache-only candidate-index drift; audited recovery restored the exact original staged index/tree and left the readiness artifact absent. The earlier 161-file source seal was invalidated. A real-verifier regression then proved that `git write-tree` could not use the live index while the conventional lock existed, superseding Revision 25's GO. Gate root causes were recursive outer-sandbox integration, an implicit conformance launcher, an incomplete timeout hierarchy, unmasked `/var/tmp`, and recursive host `/dev` exposure. The repaired sandbox binds its outer marker, identity, private Mix paths, pinned launcher, private temp/device roots, and 2,400/2,100-second timeout hierarchy. The publisher holds the conventional fence before verification, uses semantic candidate identity plus a per-attempt raw digest, and routes all verifier Git work through a private transaction-owned index. Independent publisher and sandbox rereviews are GO; the current publisher slice passes 23 tests, the heavyweight clean integration passes in 217.344 seconds, and the complete 131-test pre-seal harness passes in 783.624 seconds. Frozen-tree publication and exact staged full gates remain pending.
- Revision 27 — 2026-07-18: classified the R0-06 source-bound fixture expansion that Revision 26 omitted. All 23 R0-05 fixture files remain in original relative order; exactly 14 named R0-06 tests are added, raising the exact oracle from 411 to 536. The child deadline rises from 300 to 600 seconds while its private immutable snapshot, 1 MiB output bound, process-tree cleanup, exact summary, and no-disabled-outcome rules remain enforced. Provenance now binds the readiness compiler, its tests, and the hook tree. The first repaired-tree 161-file/536-test reseal at source SHA-256 `6dc536519cb863f471fb2b3b6b03bfc6b18cc210181ff90056d588c7b64dbea9` passed but was intentionally invalidated by this documentation repair; the classified tree requires a fresh reseal before publication.
- Revision 28 — 2026-07-18: rejected the second atomic readiness pair as R0-06 acceptance even though publication and pair verification completed, because `no_model_live_discovery`, both Python harness gates, and `upstream_make_all` remained deterministically blocked. Exact isolated diagnostics found an optional-success receipt emitted as `available` instead of `pass`, the clean-integration outer-sandbox exit after host-only setup, package-cwd rejection under `make all`, test-only stat-cache writers inheriting `GIT_OPTIONAL_LOCKS=0`, and a runtime-paired manifest not being fully unsealed before private fixture replay. The first helper-only unseal regression was then rejected by independent review because production sealing did not call it before candidate construction. The narrow repairs normalize successful receipts, validate the outer repository plus exact workspace/package cwd before host setup, explicitly enable optional locks only for the two writer simulations, and apply a runtime-reset unsealed projection before both production test and publication candidates. Blocked/pass prior-runtime integration cases and all 13 seal-transaction tests pass. Earlier seals and the blocked published pair remain rejected; a fresh classified seal and authoritative publication are required.
- Revision 29 — 2026-07-18: rejected the first classified-tree pre-seal and exact-sandbox `make all` attempts after the 536-test fixture independently produced request-timeout outcomes in two existing immediate fake-I/O Connection oracles instead of their byte-cap and duplicate-response classifications. Both affected tests pass 20 same-VM repetitions and the complete 58-test Connection file passes, isolating whole-fixture scheduling slack rather than transport logic or file-local state. Only those two positive fake-I/O waits, plus the paired unexpected-ID request in the same oracle, rise from 2 to 5 seconds; production code and the explicit 55/100/300-millisecond absolute-deadline tests remain unchanged. Under deliberate post-repair contention, the 20-repeat pair remains green while the complete Connection file passes 58 tests in 95.9 seconds. The 600-second fixture process deadline, exact 536-test summary, and no-disabled-outcome rules remain intact. Fresh full replay is required.
- Revision 30 — 2026-07-18: replaced the unchanged pinned-upstream Linear project binding with the fork owner's dedicated downstream validation-project slug, preserving the existing active-state policy and the exact terminal names including both cancellation spellings. The release-only read probe is being strengthened to require two bounded Backlog fixture shapes, an actual comment, an intra-pair `blocks` relation, and a stable repeated snapshot without committing or emitting team, issue, account, workspace, credential, comment, or relation identities. The ordinary task remains usable against arbitrary valid projects, every external operation remains read-only, and mutation capability remains schema-only. The bounded downstream live probe and all final source-bound gates remain pending.
- Revision 31 — 2026-07-18: completed the bounded downstream pre-freeze Linear probe after repairing Req-only runtime startup, the 16-node nested connection bound, and exact URL-selector/returned-`slugId` suffix verification. Project/team visibility, configured states and labels, both Backlog fixture shapes, an actual comment, an intra-pair blocker, and the unchanged repeated snapshot passed; all external operations were queries, mutation evidence remained schema-only, and no mutation occurred. The source-bound child now pins the dev Mix environment and force-compiles with warnings as errors. Focused regressions prove journal/backup retention through final verification, maximum-plus-one archive rejection before sorting, and dependency PLT/hash filename-version consistency. R0-06 remains in progress pending the final sealed replay and complete acceptance gates.
- Revision 32 — 2026-07-18: rejected the first post-adaptation seal after its immutable snapshot completed 543 tests with zero failures but the stale exact-summary oracle still expected 536. The seven added tests live inside the already classified Linear capability files, so the fixture inventory and 600-second bound are unchanged. The source oracle, bundle regression, checkpoint, journal, threat model, and current ledger entry now bind all 543 tests; no stale-oracle seal was published and a fresh exact replay remains required.
- Revision 33 — 2026-07-18: rejected the first post-seal no-model Codex replay before App Server startup after the daemon restart left `MISE_DATA_DIR` unset and the private child therefore attempted an offline tool install beneath its replaced `HOME`. The repair derives and validates the explicit or standard mise tool root before replacing `HOME`, copies only bounded Hex archives and exact versioned `rebar3` into private Mix paths, and fingerprints them after every setup step and the no-model task. Independent security review also found that the standalone Linear child copied the entire shell-sourced environment; it now forwards only the required Linear key and an explicit safe tool/proxy/certificate allowlist. Its temp and Erlang crash-dump paths are always private and ephemeral, preventing a credentialed VM crash from persisting a default dump in the repository. Three focused boundary tests pass, neither failed setup reached a capability request nor a model turn, and the preceding seal/replays are invalidated pending fresh exact evidence.
- Revision 34 — 2026-07-18: rejected the next standalone no-model replay before App Server startup after its corrected private tool setup reached lock-checked dependency resolution but the standard host Hex cache lacked package tars previously selected through an ambient Hex-home hint. The first of the existing three setup commands may now bootstrap the exact locked packages into private Hex/dependency roots; dependency compilation, application compilation, and the capability task remain explicitly offline. Focused tests bind that transition, no capability request or model turn occurred, and the preceding seal/replays are again invalidated pending fresh exact evidence.
- Revision 35 — 2026-07-18: an independent security review rejected the first online-bootstrap repair because probe-created auth and identity copies were already present in the temporary root traversable by the networked child, while the writable task snapshot was not itself reverified after setup. The repaired standalone path runs the sole networked lock-check from a separate minimal staged-source root before any probe-created credential root or copy exists, compiles only Hex-offline with a private erlexec source, and installs verified credentials into a fresh sibling root only after all setup boundaries pass. Exact checkout inventory, modes, and Git blob IDs are rebound before and after all three setup commands and the authenticated task; private dependency/Mix/rebar build fingerprints, `app.config --no-compile`, and a no-build-preamble rule prevent credentialed recompilation. A second staged-tree handshake, recomputed synthetic schema basis from the current tree bytes, and exact static-source comparison block index/source/manifest races before setup and again on the snapshot. The focused readiness class passes 25 tests, including source-mismatch/index-race/schema-basis, setup-failure/no-credential-install, byte/mode/delete/symlink/extra-entry snapshot, dependency/build-root, and authenticated-recompile negatives. Focused independent security and test reviews are GO; source sealing and authoritative no-model replay remain pending.
- Revision 36 — 2026-07-18: the first post-Revision-35 sealed no-model replay reached Hex-offline application compilation but stopped before credential installation because the generic private-build fingerprint rejected 14 normal pinned Mix compiler links. The repair keeps every non-build inventory symlink-rejecting and admits only exact compiler-owned Mix app-source, Phoenix colocated, and internal Rebar profile-plugin forms. Relative text must equal the exact compiler-derived `relpath`; an absolute Rebar link must equal its exact internal target but is normalized in the digest. Targets remain bound to the exact source, dependency, erlexec, Mix-build, or Rebar-build roots; only the exact Phoenix `assets/node_modules` target may be missing. A crafted second-permitted-link plus `..` substitution is rejected, link text is size-bounded before reading, and link identity/text/presence are digest-bound. The focused readiness class passes 27 tests and the complete readiness script passes 46 tests in 222.351 seconds. Fresh independent security and test reviews are GO with no remaining P0/P1/criterion-impacting P2; the repaired source invalidates the preceding seal and Linear replay, so final exact-tree gates remain pending.
- Revision 37 — 2026-07-18: rejected the next no-model result even though the authenticated task emitted its final no-model record, because Mix discovered the project-defined task before applying its `app.config --no-compile` requirement and re-entered erlexec dependency compilation after credentials were installed. A credential-safe invalid-argument diagnostic reproduced only the classified Mix/Rebar build-output pattern without starting App Server. The first repair used Mix's built-in `run` task with all documented deny flags and passed the focused 27-test class plus the complete 46-test readiness script in 220.136 seconds, but independent security review rejected it: pinned Mix still loads dependency metadata and evaluates the vendored erlexec `rebar.config.script`, including its silent shell command, after credentials are installed; a project `run` alias could also supersede the built-in task. The apparent green checks did not exercise those paths, so that repair and its evidence are rejected.
- Revision 38 — 2026-07-18: replaced the rejected project-aware Mix runner with the absolute pinned Elixir entry script, an empty mode-`0700` non-project cwd, a finite sorted set of real compiled `ebin` directories, the verified Codex native executable, and a static call to `run_sealed/1`. The task environment drops all Mix, Hex, mise, Rebar, and erlexec build selectors before credentials are introduced; pinned Elixir/Erlang entry scripts, core beams, and VM executables plus compiled code roots are fingerprinted before and after execution. A credential-canary integration creates a hostile Mix project, `run` alias, runtime config, Rebar script, and rogue Codex command and proves none executes. The focused readiness class passes 28 tests; complete readiness replay, fresh independent review, resealing, and authoritative live probes remain pending.
- Revision 39 — 2026-07-18: rejected Revision 38 as complete evidence after review found that its empty runner cwd left `Config.settings/0` to search for a nonexistent `runtime/WORKFLOW.md`; the hostile integration used a printing stub and could not expose that composition failure. The sealed task now requires the canonical regular `<snapshot>/elixir/WORKFLOW.md` as separate argv, binds it only around collection with guaranteed restoration, and keeps the Elixir runner in the empty private cwd while the verified native Codex child intentionally probes the immutable snapshot cwd. A new noncredentialed regression clears the inherited environment, invokes the actual compiled production `run_sealed/1` through the pinned absolute Elixir runner from an empty mode-`0700` directory, and completes workflow parsing, config resolution, isolated erlexec startup, and all nine fake App Server receipts. The classified fixture oracle rises from 543 to 544. The first full readiness run exposed two old private-Mix-tool fixtures that omitted the newly required pinned Erlang line; after that fixture-only repair, all 47 readiness tests pass in 227.691 seconds. The 28 focused readiness tests and five capability-task tests pass, and both fresh independent reviews are GO with no P0/P1/criterion-impacting P2. Resealing and authoritative read-only probes remain pending.
- Revision 40 — 2026-07-18: rejected the first Revision 39 atomic pair as acceptance even though the 544-test source seal, installed 0.144.3 verification, deterministic regeneration, protected Linear query replay, quota-protective no-model replay, publisher transaction, and every other required conformance row passed. `schema_harness` and `upstream_make_all` alone blocked because the intentional Python discovery guard still expected the historical 131 tests while the classified tree discovers exactly 143: 96 schema/compiler tests plus 47 readiness tests. Direct replay ran all 96 schema/compiler tests in 575.451 seconds and its sole error was that stale guard; `make all` calls the same runner first. The guard now binds exactly 143 tests without removing, skipping, or suppressing any test. This edit invalidates the rejected pair; a fresh seal and complete authoritative replay remain required.
- Revision 41 — 2026-07-18: rejected the second atomic pair even though its fresh 544-test seal, installed-pin verification, deterministic regeneration, protected Linear query replay, quota-protective no-model replay, repaired 143-test schema harness, publisher transaction, and every required row except `upstream_make_all` passed. A solitary exact-sandbox diagnostic returned exit 2 after 1,257.930 seconds, with all 143 Python tests green in 563.888 seconds followed by five unsuppressed Dialyzer warnings. The narrow repairs preserve bounded behavior while removing opacity-triggering cursor sets, a redundant typed map branch, a duplicate state nonempty check, and unreachable typed-response fallbacks. Direct Dialyzer and the complete 126-test affected behavior slice pass. The complete build now has a distinct finite 1,800-second deadline, while offline byte-stability replay retains 1,200 seconds. These edits invalidate the pair; fresh seal, publisher, smoke, hygiene/archive, and exact-tree review evidence remain required.
- Revision 42 — 2026-07-18: the sealed Revision 41 predecessor tree passed a source-bound no-model Codex replay with eight ordered read-only receipts and no thread or turn, then passed all 13 commands in a non-publishing authoritative inventory at source SHA-256 `d350da5ce3b14e1fdd970f0f880f610b4a33970abc7c7f02562512e8b705ace3`. The green inventory includes the 143-test schema harness, 544-test replay, reproducible archive, repaired private `make all`, complete version-consistent PLT/hash inspection, and offline Dialyzer byte replay. Its Linear command passed the fail-closed report contract without making a request, but all seven Linear capability statuses remained blocked because neither a process nor a bounded readable host environment file exposed `LINEAR_API_KEY`. The candidate correctly remained `blocked_r0_06`. This documentation edit invalidates that seal; final acceptance requires the protected assignment, fresh seal, query-only Linear replay, atomic publication, smoke, hygiene/archive, and exact-tree reviews.
- Revision 43 — 2026-07-18: repaired two criterion-impacting evidence defects found by independent review. The machine-read ledger header had remained at Revision 39 even though history extended through Revision 42, which would have made the final readiness manifest's exact patch-ledger revision false; the header now advances with this Revision 43 record. The R0-06 checkpoint also no longer contradicts its current protected-assignment blocker by calling Linear access unblocked: it distinguishes the successful pre-freeze project/fixture proof from the unavailable current credential path. No runtime, configuration, or acceptance claim changes. This documentation repair invalidates the prior seal and remains subject to the full final sequence.
- Revision 44 — 2026-07-19: replaced failed inherited-pointer polling with an authorized command-scoped protected boundary. A temporary owner-only launcher outside Git validated a bounded regular non-symlink credential file, devbox ownership, mode `0600`, and exactly one non-empty key assignment in memory; removed the pointer; and exposed only `LINEAR_API_KEY` to the exact readiness process and its allowlisted Linear child. The source-bound query-only replay passed project/team binding, configured states, labels, both Backlog fixture shapes, an actual comment, the intra-pair blocker relation, stable repeated viewer/project/fixture reads, and schema-only mutation introspection. Every provider operation was a query; no Linear mutation occurred and neither fixture changed. No protected value or path entered output, evidence, or Git. This is the final R0-06 acceptance freeze: its accepted statuses are valid and may be committed only if this exact documentation tree passes the fresh 544-test seal, installed Codex verification, deterministic regeneration, quota-protective no-model replay, all 13 atomic-publisher commands including repaired private `make all` and independent offline Dialyzer replay, atomic pair verification, no-model smoke, hygiene/archive checks, and two fresh exact-tree reviews with no unresolved P0/P1 or criterion-impacting P2. A failure rejects this revision before commit and requires a new freeze.
- Revision 45 — 2026-07-19: two fresh independent reviews of the exact Revision 44 staged tree rejected its acceptance claim. They found two P1 credential-boundary defects: the outer capability namespace exposed copied Codex auth/identity to candidate-controlled setup commands, and the Linear key entered a single Mix compile-and-probe process. A third P1 found that the Python projection could discard hidden/reference evidence and accept a `blocked` reference profile that did not bind to the visible models and readiness auth/identity result. A criterion-impacting P2 found checkpoint, threat-model, and ledger wording that contradicted the claimed Revision 44 artifact. The Revision 45 candidate makes generic/build sandboxes structurally credential-blind; runs six credential-free setup phases with only the dependency and test-NIF bootstrap phases networked; introduces separate direct sealed Codex and Linear entries after the frozen build; and retains and validates `referenceProfile` against visible model/effort, ChatGPT-authentication, and identity evidence. The readiness suite passes 51 tests and the focused Linear slice passes 31 tests, both with zero failures. The first complete schema-harness attempt ran all 147 Python tests and its immutable fixture passed 546 Elixir tests, then the exact-summary oracle correctly rejected the stale expected count of 544; the bound now advances to 546. The first corrected seal then exposed one full-suite-only five-second positive-event scheduling miss in a retained cancellation test and cascading fail-closed runtime timeouts; the exact case passes 1/0 and the complete file passes 22/0 at seed 0, so only that test wait rises to 15 seconds without changing production or aggregate deadlines. The repaired pre-freeze source seal subsequently passes all 546 tests and publishes the verified 0.144.3 manifest; this documentation receipt intentionally requires one final exact-tree reseal. Revision 45 remains unaccepted: it requires that final reseal, the complete 147-test harness, authoritative full gate and publication, smoke/hygiene/archive checks, and fresh exact-tree final reviews before any row may return to `Accepted` or R0-06 may be committed.
- Revision 46 — 2026-07-19: the first two protected Revision 45 Linear retries stopped before credential installation or a provider request because the compiled runtime validator found an empty required `symphony_elixir` application root. A credential-free trace isolated the cause: all six setup commands still mounted the minimal dependency-only source, so the final offline application compile could succeed without compiling the immutable staged application. The repair retains that overlay for the five dependency phases and removes it only for the sixth warnings-as-errors application compile; private writable dependencies/build outputs, offline isolation, and source/build fingerprints remain unchanged. The exact mount-selection regression passes 1/0 and the empty-root validator remains fail closed. The corrected command-scoped protected replay then passed project/team visibility, configured states, labels, both fixture shapes, comment and blocker reads, stable repeated results, and schema-only mutation evidence. Every external operation was a query; no mutation occurred and neither fixture changed. The complete repaired readiness suite passes 51/51 in 237.785 seconds. Revision 46 requires the final exact staged-tree seal, authoritative publisher including the final protected query-only replay, and fresh exact-tree reviews.
- Revision 47 — 2026-07-20: the first authoritative Revision 46 publisher atomically staged and verified a truthful blocked pair with 11 passing conformance rows; `schema_harness` and its dependent `upstream_make_all` row were blocked. The exact schema command passed directly at 147/147 in 811.807 seconds. A credential-free reproduction inside the exact private gate sandbox failed in 59.340 seconds because the real snapshot fixture discarded the verified outer offline Hex cache and Mix tooling, attempted `builds.hex.pm`, and received `nxdomain`. The first repair preserved ordinary standalone network bootstrap and, only under the validated outer marker, forwarded the verified `HEX_HOME`, `MIX_ARCHIVES`, executable `MIX_REBAR3`, and `HEX_OFFLINE=1`; this removed the network attempt but a second exact private replay failed in 59.694 seconds with `:eaccess` because offline Hex writes registry state while the outer cache is read-only. A bounded no-follow private Hex-cache clone repaired that boundary while keeping Mix archives and Rebar read-only. The third exact replay passed resolution but failed in 160.517 seconds because the nested XDG cache lacked the checksum-bound `lazy_html` precompiled NIF archive from the credential-free bootstrap. The final boundary also clones only a bounded flat inventory of regular `.tar.gz` NIF archives into the nested private cache, where the offline compiler retains pinned-checksum validation. Entry and byte bounds apply before sorting or copying; missing, malformed, symlinked, incorrectly typed, changed, over-bound, or non-executable inputs fail closed. The focused environment/cache oracle passes 1/0; fresh private replay, source seal, authoritative publisher, and exact-tree reviews remain required.
- Revision 48 — 2026-07-20: the final Revision 47 exact private schema reproduction passed 147/147 in 560.886 seconds. Its authoritative protected publisher repeated the query-only Linear and quota-protective no-model Codex replays and atomically staged a truthful pair with 12 passing conformance rows; Linear retained seven passing rows and `mutations.evidence=schema_only`, and no mutation occurred. `upstream_make_all` alone blocked at strict Credo before Dialyzer because `run_with_probe/4` nested report collection three levels deep. Revision 48 extracts only that collection into one private helper. All seven Linear task tests pass, and strict Credo checks 123 source files and 3,886 modules/functions with no issue. The blocked pair is rejected and invalidated; fresh seal, authoritative publisher, pair verification, complete PLT/Dialyzer proof, smoke/hygiene/archive checks, and exact-tree reviews remain required.
- Revision 49 — 2026-07-20: two mandatory independent reviews rejected the exact Revision 48 green pair for four criterion-impacting P2 defects. The strict checkout inventory/OID/source validator and private Mix/Rebar build-byte/link validator were implemented and unit-tested but not called by the authoritative live/full-gate paths; the protected identity key and parent lacked current-user ownership enforcement in both Elixir and the readiness copier; and the checkpoint contradicted its Revision 48 header with `Candidate revision: 45`. Revision 49 replaces the weak post-checkout baseline with complete staged-OID/inventory/source inspection before and after execution, validates and fingerprints the complete private build around authenticated tasks and every exact gate, requires current-user-owned protected key inputs and parents, and binds checkpoint/header/ledger provenance to Revision 49. A complete readiness attempt and real-sandbox targeted replays further exposed that valid Mix links are nested under an environment prefix and spell targets relative to the sealed namespace rather than host mount paths. The final classifier validates the real host-owned target bytes while requiring those exact sandbox lexical links; positive/adversarial link cases pass 2/2 and the real private-sandbox regression passes 1/1 in 229.836 seconds. No Linear mutation or model work was executed during review or repair. One fresh authoritative publisher and only the invalidated exact-tree reviews remain required.
- Revision 50 — 2026-07-20: the first Revision 49 authoritative publisher failed closed before publication at final strict checkout replay. The initial oracle matched the exact 2,108 staged files and 69 staged parent directories; sandbox preparation then added three deliberate unindexed empty mountpoints for private Git, coverage, and escript outputs. Revision 50 requires each gate-owned mountpoint to remain unindexed, current-user-owned, mode `0700`, and empty, removes exactly those three after the last sandbox use, and then repeats the complete inventory/mode/blob-OID/source oracle. Missing, linked, nonempty, reowned, or remoded mountpoints block publication. The focused prepare/tamper/cleanup/exact-replay regression passes 1/1. The failed transaction restored the original staged tree, accepted no evidence, spent no model turn, and executed no Linear mutation. One fresh authoritative publisher and only the invalidated exact-tree reviews remain required.
- Revision 51 — 2026-07-20: the Revision 50 publisher passed strict mountpoint cleanup and staged a truthful blocked diagnostic pair with 10 of 13 rows passing. Installed verification and deterministic regeneration rejected fixture evidence that still described the pre-Revision-49 readiness compiler/tests, and the dependent full quality row remained blocked. Independent pair verification passed, while direct installed verification reproduced the stale fixture-source digest. Revision 51 changes no runtime behavior and restores the required evidence order: execute the exact 546-test fixture reseal on the repaired source before one fresh authoritative publisher. That reseal now passes 546/546 and binds 161 fixture-source files at SHA-256 `56445b3ecb7d1fa2ecea3e1e3d9ce366f311c50c96b4af05d7ff3da6c2870efc`; installed verification binds 1,873 schema files, and deterministic regeneration matches every semantic/raw checksum. The blocked pair is rejected evidence. Its Linear replay retained seven query-only passes and schema-only mutation evidence; no Linear mutation or model turn occurred. The final publisher and invalidated exact-tree reviews remain required.
- Revision 52 — 2026-07-20: the Revision 51 publisher passed 12 of 13 rows; `upstream_make_all` alone blocked. A credential-free exact-sandbox diagnostic returned 2 at strict Credo before Dialyzer because the current-user ownership branch made private `load_key/1` depth three with cyclomatic complexity eleven. Revision 52 extracts validation and file reading into two private helpers without changing results. The identity slice passes 9/9, public specs pass, and strict Credo checks 123 files and 3,890 modules/functions with zero issue. Its exact source-bound reseal now passes 546/546 and binds 161 fixture-source files at SHA-256 `66df31374ef1e140d8b52e21a3be6a9ea97191a101d4815a844a8bdf71923f66`. The prior seal and blocked pair are rejected; the final publisher must repeat. The rejected pair's Linear replay retained seven query-only passes and schema-only mutation evidence; no Linear mutation or model turn occurred.
- Revision 53 — 2026-07-20: the Revision 52 publisher passed all 13 conformance rows, the independently verified readiness/schema pair reported no blocker and runtime pass, and the separate no-model smoke started no model work and cleaned all owned resources. The final public-history hygiene gate then rejected the candidate solely because one Python test spelled the protected pointer variable literally. Revision 53 composes that test-only key at runtime while preserving the exact credential-blind environment assertions; both affected tests pass 2/2 and the prohibited literal is absent from the worktree source. Because the test belongs to the 546-test fixture source set, the Revision 52 pair and smoke receipt are rejected as final evidence. The Revision 53 reseal now passes 546/546, binds 161 exact fixture-source files, and records source SHA-256 `cef2ebf983dc5f32d06721707568f46ecdb91bb258ac801b4cc58aaa3a456091`; the authoritative publisher, smoke, hygiene, reproducible-archive comparison, and invalidated exact-tree reviews remain required. No Linear mutation or model work occurred during this repair.
- Revision 54 — 2026-07-20: Revision 53 passed all 13 conformance rows, pair/schema verification, no-model smoke, hygiene, and two byte-identical archives on staged tree `ad14bd74c85b6755da554ac7bcbb2bfd8cdefbb8`. Reviewer A returned GO with no finding. Reviewer B independently found one criterion-impacting P2: the intentional downstream modification to upstream `elixir/WORKFLOW.md` lacked the prominent per-file change notice required by Apache-2.0 section 4(b), the release contract, and the fork policy. Revision 54 adds a comment-form copyright, SPDX identifier, and explicit downstream modification notice immediately inside the preserved YAML front matter; no tracker or executable workflow value changes, and the focused current-workflow parse check passes 1/1. Revision 53's pair and both review receipts are rejected as final evidence. The Revision 54 reseal now passes 546/546, binds 161 exact fixture-source files, and records source SHA-256 `1a86376180d351b480ca8db1eedda8b9afc14fe7365f100378e6274540fabe33`; the authoritative publisher, smoke, hygiene, archive comparison, and both exact-tree reviews must repeat.
- Revision 55 — 2026-07-20: Revision 54 subsequently passed all 13 authoritative rows, pair/schema verification, no-model smoke, hygiene, and two byte-identical archives on staged tree `2243f718108242d969cbb9eaeb17ee09b26e58b0`. Reviewer B returned GO. Reviewer A found one P1: the out-of-tree launcher injected the raw Linear key into staged publisher and BEAM processes that retained direct egress, so actual query-only behavior did not make mutation technically impossible. Revision 55 rejects that evidence and replaces the handoff with an explicit trusted out-of-tree supervisor/broker. It masks the credential source and host process space from a credential-blind private publisher; credential-free dependency bootstrap retains its required network. The raw key stays only in the non-dumpable broker, which accepts only the exact sealed BEAM peer after checking its executable, code-path/entrypoint suffix, absent key/pointer environment, distinct PID/mount/network namespaces, loopback-only interfaces, empty route table, and fixed socket inode. That networkless peer receives the one fixed private Unix socket. The broker owns the canonical Linear endpoint, six exact query documents, public project/team/fixture variable policy, learned team binding, response bounds, exact nine-query transcript, and chained zero-mutation receipt; query text, URL, method, header, auth, arbitrary operation name, reorder, replay, duplicate-key, oversize, redirect, compressed, and extra inputs fail closed. The affected Elixir slice passes 34/34, four staged Python boundary tests pass, and the external five-test adversarial suite passes. The exact fixture now expects 549 tests and the complete schema harness 150; seal, authoritative publisher, post-publication checks, and two fresh reviews remain required. No real provider request, mutation, or model work occurred during repair.
- Revision 55 fixture-path repair — 2026-07-20: the first complete repaired schema harness ran all 150 Python tests and all 549 immutable Elixir tests, then rejected one error in the new broker transport test. The test derived its AF_UNIX filesystem-socket pathname from the fixture's deliberately deep temporary root and exceeded the kernel pathname limit; the fixed production sandbox socket was unaffected. The test now selects a unique cleanup-owned short `/tmp` socket. Its direct two-test replay, formatting, warnings-as-errors compilation, and strict Credo pass. The rejected harness made no provider request or mutation; the exact staged source reseal and single final authoritative publisher must repeat the complete harness.
- Revision 56 — 2026-07-20: the repaired Revision 55 source seal passed 549/549 over 163 exact fixture-source files; installed Codex `0.144.3`, deterministic regeneration, the complete 150-test harness, the brokered nine-query/zero-mutation replay, the private full gate, complete PLT/Dialyzer proof, and all 13 publisher rows passed. The pair was atomically verified on tree `bab14a649842d8133d0db9a48c32d78173c75201` with content-free broker receipt `e4305d7ba7e935eca7b478ec8ce0697f8c6375b1f5e5ea31241426a2f3811cc1`. A final pre-acceptance audit rejected that pair because the trusted outer child command still contained the exact protected pathname and public documentation incorrectly described the credential-free bootstrap publisher as networkless. No credential value or raw response was exposed, neither fixture changed, and no Linear mutation occurred. Revision 56 masks the protected credential's whole parent, keeps its exact pathname out of child arguments, privately copies and verifies only Codex auth for the publisher, and corrects the checkpoint, journal, threat model, and ledger to match the actual outer-network/inner-route-free boundary. The external five-test suite and explicit outer-network diagnostic pass; one exact Revision 56 seal, publisher, post-publication sequence, and both fresh independent reviews remain required.
- Revision 57 — 2026-07-20: the exact Revision 56 seal passed 549/549, and its corrected brokered Linear replay again passed all public rows with exactly nine fixed queries, zero mutations, and unchanged fixtures. The solitary publisher completed its gate inventory but failed closed before pair construction: the no-model Codex row was classified blocked, then `_compile_full_public_report` substituted the `not_run` reference-profile default into a full report whose allowed statuses are only `pass` or `fail`. The resulting vocabulary exception masked the blocked conformance row and no pair was published. An immediate standalone source-bound no-model probe passed the installed `0.144.3` contract, eight read-only receipts, the complete passing reference profile, and `noModelWork=true`, with no thread or turn. Revision 57 uses the existing visible-model/auth/identity truth function for the absent-live fallback, yielding an all-false `fail` profile and a valid blocked pair rather than an exception. The exact regression and broader full-compiler slice pass; no model work or Linear mutation occurred. Fresh seal, one final publisher, affected post-publication checks, and both exact-tree reviews remain required.
- Revision 58 — 2026-07-20: the Revision 57 reseal passed 549/549 over 163 exact fixture-source files. Its protected publisher passed every deterministic row and the brokered Linear row, completed exactly nine fixed queries with zero mutations and unchanged fixtures, and atomically published a valid `blocked_r0_06` pair whose only blocked row was `no_model_live_discovery`. A targeted credential-free replay of the exact staged Codex probe under the same outer namespace returned `credentials_unavailable`: the fresh tmpfs masking the protected credential parent defaulted to mode `0755`, while the sealed verifier correctly requires the remounted auth file's direct parent to be owner-only `0700`. Revision 58 changes only that trusted out-of-tree mount boundary to chmod the fresh tmpfs `0700` before mounting the privately verified auth copy and binds the order in the external five-test adversarial suite. The identical exact outer-namespace probe then exited zero with the expected public record. The Revision 57 blocked pair is rejected; no model work, Linear mutation, fixture change, credential value, or protected pathname was exposed. The exact Revision 58 source seal passes 549/549 over 163 fixture-source files at SHA-256 `5ad8656cf52629ada54f8b70d15fa3cd376bb2892f50122ddb43d995dae75ccb`; one final publisher, affected post-publication checks, and both exact-tree reviews remain required.
- Revision 59 — 2026-07-20: both mandatory independent reviews rejected the exact green Revision 58 staged tree `36b279f9b8c0b9e45a6061d4342047213cc7aa88`. The security review found one P1: generic candidate-aware gates could replace a valid `mix-build/dev` BEAM, after which the full-gate loop accepted the changed complete-build fingerprint as its next baseline and later exposed Codex credentials plus network to that altered direct runtime. It also found one criterion-impacting P2: auth was copied before identity inside `_install_private_live_credentials/4`, but the caller's cleanup scope began only after installation, so a second-write failure could retain the first copied credential. The evidence review found a separate criterion-impacting P2: the ledger header still reported Revision 55 while history and the green readiness artifact described Revision 58, and the compiler checked only header syntax rather than the latest normative history record. Revision 59 seals every compiled `dev` ebin byte loaded by the direct Codex or Linear tasks immediately after credential-free bootstrap, never advances that credential-runtime baseline after generic gates, and rechecks it immediately before and after both sealed live tasks. Credential installation is transactional and removes a partially created root on every ordinary installation failure. The ledger parser now requires every normative revision line to parse, requires monotonic history including qualified same-number repairs, and rejects a header that differs from the final history revision. Focused negative regressions pass 3/3 and prove runtime drift blocks before credential installation/direct execution, second-write failure leaves no credential root, and a stale ledger header fails closed; the retained direct-child cleanup regression also passes. The exact Revision 59 reseal passes 549/549 over 163 fixture-source files at SHA-256 `938c13ea29d4f01e34e7fe548889bdd7a98d150027c499a0f1732de7f18f9f98`. Revision 58's pair, post-gate receipts, and both review receipts are rejected as final evidence; Revision 59 requires the one complete authoritative publisher, affected post-publication checks, and two new exact-tree reviews.
- Revision 60 — 2026-07-20: the two mandatory Revision 59 reviews were independent: the evidence reviewer returned GO, while the security reviewer rejected exact staged tree `069e66aa84b8a055ba583fd7c8bf4ef9a4901c6c` with two P1 findings. The network-enabled staged publisher could still read the Codex auth copy mounted by its trusted launcher, and the Linear broker's lexical BEAM shape attestation did not cryptographically bind its raw-response peer to the supervisor-launched executable and compiled code roots. Revision 60 moves all credential ownership, raw Linear bodies, and direct-child launches behind one external trusted supervisor. The staged publisher receives only an owner-only fixed control socket and private work root, sends only public hashes/tree/name/tool fields, and rejects any credential selector. The supervisor independently binds the exact index snapshot, runtime roots, pinned tools, installed Codex bytes, child lineage, BEAM executable bytes, code-root bytes, namespaces, route table, socket inode, output bounds, process cleanup, and strict public projections. Only a disposable supervisor-owned Codex credential copy is writable inside the Codex child; only the separately networkless Linear child can reach the raw-response broker. A hostile networked-publisher canary proves that no credential path, environment value, argument, or descriptor is visible. The readiness suite passes 56/56 and the external adversarial suite passes 14/14. Command-local selector injection was required because the parent tool environment filtered it; the selector stayed in the trusted supervisor only. The protected targeted Linear replay verified the dedicated project/team, configured states, labels, both fixtures, comment, blocker, stable repeated result, and schema-only mutation capability with exactly nine queries and zero mutations. The protected no-model Codex replay verified installed `0.144.3`, eight read-only receipts, the passing reference profile, and `noModelWork=true`. The exact Revision 60 fixture reseal passes 549/549 over 163 source files at SHA-256 `bd4a88e3ef42e0efabc1a440277520cfb54b37f106c35e190184f81816cf5ba8`. Revision 59's pair and reviews remain rejected, and Revision 60 still requires one authoritative publisher, affected post-publication checks, and two new exact-tree reviews.
- Revision 60 provider-replay recovery — 2026-07-20: the first frozen Revision 60 publisher passed all 12 deterministic rows, including the 153-test schema harness, private `make all`, offline Dialyzer/PLT proof, source archive rehearsal, and protected nine-query/zero-mutation Linear replay, then atomically staged a truthful blocked pair because the no-model Codex child received a transient transport failure on optional `account/usage/read`. The external supervisor rejected overall success; the blocked pair at staged tree `463cac5781d4cd526a72a50ea3a99e1230a6ed94` is not acceptance evidence. One immediate targeted no-model replay on the identical source identity passed all eight ordered read-only receipts, the complete reference profile, and `noModelWork=true`, including the previously failed usage read. No source defect, model work, Linear mutation, or fixture change occurred. This material live-evidence change permits one final complete publisher replay under the no-redundant-gate policy; the blocked pair and targeted recovery result do not replace that final coherent pair.
- Revision 61 — 2026-07-20: the final Revision 60 publisher passed all deterministic and protected live rows and atomically staged a green pair on tree `5f2ae89e4952d79494d381dae3e147fbb84a9301`. Independent pair verification, installed Codex `0.144.3` verification, two-pass archive reproduction, and the initialize/initialized-only production smoke passed. The required public-history hygiene gate then rejected the pair because the staged publisher's fail-closed environment check spelled the protected Linear selector identifier literally. No credential value or protected pathname was present, the Linear transcript remained exactly nine queries and zero mutations, neither fixture changed, and no model work started. Revision 61 preserves the same fail-closed check by constructing that identifier from non-sensitive components at runtime and expands the focused regression to prove both raw-key and protected-pointer selectors are rejected without placing the protected identifier in public history. The focused regression passes 1/1 and the previously failing hygiene gate passes on repaired tree `18857842c3834716f3f492b0aae2575f14cc16bf`. Its exact source-bound reseal passes 549/549 over 163 files at SHA-256 `e6ee5b7239e3a8d9db9bfc798b48fadbde6f550b6a1b3aff26028a0d25fe3816`. The Revision 60 pair and post-gate receipts are rejected; Revision 61 requires one complete publisher, affected post-publication checks, and two fresh exact-tree reviews.
- Revision 62 — 2026-07-21: both mandatory Revision 61 reviews reproduced exact green tree `eb78ad81ad68c4b61c89d204ba40623b3a815bfe`. The evidence reviewer returned GO; the independent security reviewer rejected it with one P1. The network-enabled staged publisher retained a writable view of the supervisor work root while the credentialed child received read-only binds of the same underlying snapshot, Mix build, dependencies, and erlexec source. Read-only bind semantics did not block writes through the publisher's other mount. The credential-runtime seal covered `ebin` roots but omitted the native `exec-port`, and the trusted protocol merely syntax-checked an unused full-build fingerprint. A publisher helper could temporarily replace the native wrapper, read the disposable Codex credential inside the child, use the child's network route, restore the shared bytes before postchecks, and leave green evidence. Pre/post hashing shared paths is insufficient because the same tamper/restore race applies to `ebin` and Linear peer-attestation inputs. Revision 62 bumps the trusted protocol, removes the misleading unused build-hash field, expands the credential-runtime digest to every `ebin` byte/mode plus the exact native port tree, and requires the external supervisor to reconstruct an exact Git-index workspace plus copy the executable runtime into a unique publisher-invisible request root before any credential preparation, key-content read, provider access, or child launch. Children bind only that private workspace/runtime; Linear code paths and peer fingerprints derive from it, and every request root is removed before response on success or failure. A targeted repair audit then found a second criterion-impacting TOCTOU: the publisher recaptured `indexTree` at supervisor contact, so a transient index swap could make private execution disagree with the earlier source/static basis and later be restored. The repaired publisher carries the gate's originally captured tree; the supervisor independently pins its startup tree before publisher launch and rejects a mismatch before private reconstruction or sensitive action. The complete repaired readiness suite passes 57/57 in 244.530 seconds, the post-audit affected slice passes 4/4 in 0.036 seconds, and the bounded combined-harness guard remains exactly 154 tests. The final external adversarial suite passes 23/23 in 2.276 seconds; full private reconstruction/revalidation of the staged tree passes over 2,110 entries, and the staged/external runtime digest contract matches. No provider request, model work, Linear mutation, or secret-content read occurred during repair validation. Revision 61's pair, post-gates, and reviews are rejected. Source reseal, protected replays, one final publisher, post-gates, and two fresh exact-tree reviews remain required.
- Revision 63 — 2026-07-21: the fresh targeted Revision 62 audit confirmed the original-tree repair but rejected the private seal with one P1 and one criterion-impacting P2. The network-enabled publisher still selected executable child bytes by supplying the runtime digest that the supervisor used to authorize the publisher's own staging area, while descendant-only control-peer attestation did not pin one exact publisher process. The client also waited only the direct-child 180-second bound even though the supervisor performed private reconstruction before granting a fresh 180 seconds, and daemonized broker/supervisor workers plus short best-effort joins could outlive client failure with credential-bearing work or undeleted private roots. An initial protocol-v3 direction change was rejected before provider use because returning a supervisor-computed digest to the same hostile publisher still did not establish provenance for publisher-authored bytes. Revision 63 therefore uses protocol v4: the seal request carries only protocol, operation, and the original index tree; its response carries only that tree and one opaque transient seal ID. Before publisher launch or any credential operation, the trusted supervisor reconstructs the exact startup Git tree and independently builds the complete dev runtime in a credentialless fixed-path sandbox: locked dependency acquisition is the only networked phase, followed by networkless locked replay, dependency compilation, and warnings-as-errors application compilation with a separately reconstructed writable exact erlexec source. After the builder and its process group are dead, the supervisor descriptor-projects every sorted `dev/lib/*/ebin` plus the exact native `exec-port` into publisher-invisible storage and revalidates it. No publisher work root, build, dependency, or erlexec byte participates in the seal. Later capability requests reference only the seal ID; the supervisor pins the exact publisher PID, executable, command line, namespaces, and socket identity across them. One 600-second absolute session deadline covers construction, both credential operations, validation, response, zeroization, process-group and non-daemon broker reaping, strict request-root removal, and protected-descriptor closure; the client bound is 660 seconds. The complete protocol-v4 readiness suite passes 57/57 in 240.562 seconds, including the affected four-test client/selector/ebin/native slice; the combined harness remains exactly 154 tests. The exact source-bound reseal then passes 549/549 over 163 source files at SHA-256 `8ddf4b3f798710ca6967a1be1540de7d664a0775ea1e544fa3070d832daf0a2d`, dated 2026-07-21, and truthfully resets runtime compatibility to `not_run`/`pending_r0_06`. External adversarial validation, protected replays, the one final publisher, post-gates, and two fresh exact-tree reviews remain required. No provider request, model work, Linear mutation, secret read, or Git/index mutation occurred during these repair tests.
- Revision 63 external-seal repair — 2026-07-21: the final external protocol-v4 supervisor is mode `0700` at SHA-256 `6747f3b9a84c408793d7b22f4919d54c8d589aced7c7c31d4c0c2b0d12836cd0`; its mode-`0600` adversarial suite is SHA-256 `b79bb8d575a433702efc2d052c338de95c3d7b9173a9399833c6c9c48d2c3032`. Root independently replays all 42 tests in 7.058 seconds. A real credential-free build from the exact staged tree independently reconstructs the source, runs locked acquisition plus networkless replay/compilation, projects 36 sorted runtime applications, removes the builder before sealing, and passes in 190.0 seconds. Hex 2.4.2 requires a writable project working directory for its relative extraction temporary directory, so the supervisor uses a second exact project projection with writable directories and one sorted read-only bind per tracked file; live canaries deny tracked append, chmod, unlink, and rename, while post-phase Git-object/inventory checks reject rogue regular files, symlinks, replacements, premature native output, and output tampering. The targeted rereview first exposed that the 600-second capability deadline could incorrectly kill later credential-free full gates, then exposed an early-publisher-exit timestamp race. The final conductor releases that deadline only after timely successful capability cleanup and a second cleanup check, while post-join acceptance requires non-null deadline/cleanup timestamps with cleanup no later than the deadline. Active, incomplete, errored, dead-incomplete, late, missing-timestamp, and cleanup-failed states terminate or reject. The targeted reviewer returns GO with no remaining P0/P1/criterion-impacting P2 in this repair scope. No protected file, credential, provider, Linear, model, or Git mutation was used. Protected query-only and no-model replays, one coherent final publisher, post-gates, and both mandatory fresh exact-tree reviews remain required.
- Revision 63 protected-replay socket repair — 2026-07-21: the first protected query-only retry failed closed after both credential-free builds and before child launch, key read, provider access, or mutation with the sole public category `capability_supervisor_failed`. Cleanup, the exact staged tree, and both external files remained intact. A credential-free diagnosis proved the nested Linear broker AF_UNIX pathname was 123 bytes against Linux's 107-byte pathname limit, so `bind` raised before the query-only broker existed. The final external launcher moves only that socket to an 85-byte supervisor-runtime-root path, verifies canonical parent/type/owner/mode/absence before bind, binds socket type/owner/mode/link count and parent/socket device/inode after bind, exposes only a fixed read-only alias to the exact child, and unlinks the exact inode only after listener, worker, and child teardown. Live workers, collisions, and replacements preserve the path and fail closed with fixed content-free reasons. Root independently passes the final 44-test external suite in 7.070 seconds, and a separate targeted auditor returns GO with no P0/P1/criterion-impacting P2. The final launcher is mode `0700` at SHA-256 `9dbbca0b80fec12a288c8b44b6f355c7eedaac5aef5b3bdba3484541c0320d3d`; its mode-`0600` suite is SHA-256 `145bf65e8b899c798731f29866a8360adf57b4836a9ae784e1cfdfc04a0eaa0d`. No protected content, provider request, Linear mutation, model work, or Git mutation occurred. One source-identical protected Linear replay is now permitted; the no-model replay, coherent final publisher, post-gates, and both mandatory fresh exact-tree reviews remain required.
- Revision 63 control-cancel repair — 2026-07-21: the source-identical protected Linear retry passed private socket creation and completed the direct query-only action but withheld its final public receipt with `supervisor_request_disconnected`. Exact control-flow diagnosis proves normal `QueryOnlyBroker.shutdown()` set both its internal stop and the caller-owned transaction cancellation event after the child and broker completed; `_run_control_transaction` then misclassified that internal orderly stop as a publisher disconnect before framing the response. The observed reason is reachable only after the broker completed exactly nine fixed query operations with a nonempty internal receipt; no mutation route existed or executed, but this is not accepted final evidence because the receipt was not published. The smallest external repair makes broker shutdown own only its internal stop and resource closure. True supervisor shutdown still sets the active transaction cancellation event first; the EOF watcher retains disconnect ownership; provider cancellation, non-daemon reaping, and key zeroization remain intact. Root independently passes the final 47-test suite in 7.119 seconds, and the focused auditor independently passes 4/4 and returns GO with no P0/P1/criterion-impacting P2. The final launcher is mode `0700` at SHA-256 `4128521bc86104cadc355bdf136711f13fa2c9dab359ae56df20bde174d9c26c`; its mode-`0600` suite is SHA-256 `c12fb94bf54bfe40841af392fa1860c8efbcf854bee2feb0f67c10c7476eb300`. One material-fix protected Linear retry is permitted; the no-model replay, coherent final publisher, post-gates, and both mandatory fresh exact-tree reviews remain required.
- Revision 63 protected Linear replay — 2026-07-21: the final material-fix query-only replay passes on exact staged tree `d69b86db8025f334811af4329c24a617ac6b990b`. Its public record binds source SHA-256 `5c6f8a5294bf2e90865c6ec5bc218ea535fe09faf5df86e49a57d729489d48c4` and configured project binding `linear-project-v1-d7fd4776f5d4cfe536ef0479c7f84611d859a570900efb4e81a7277cf400628f`; project/team connectivity, configured states, labels, both fixture shapes and stable reread, comment, blocker, and schema-only mutation evidence pass. The trusted broker emits receipt `e4305d7ba7e935eca7b478ec8ce0697f8c6375b1f5e5ea31241426a2f3811cc1` after exactly nine fixed queries and zero mutations. Neither fixture changes, no mutation executes, and the protected credential, raw responses, account/workspace identity, and private paths remain outside public evidence. The targeted no-model Codex replay, coherent final publisher, post-gates, and both mandatory fresh exact-tree reviews remain required.
- Revision 63 protected no-model replay — 2026-07-21: the targeted Codex replay passes on exact staged tree `31a8b3a7229748ea9bfb6d1140c5e8dc41efe0fa`. The supervisor independently rebuilds and seals its runtime before installing a disposable credential copy, verifies installed `codex-cli 0.144.3`, and emits eight ordered read-only request receipts. The complete reference profile passes: ChatGPT authentication and keyed identity binding are confirmed; GPT-5.6 Sol is available with review and Ultra reasoning support; required Terra shapes are available. The public record reports `noModelWork=true`; no `thread/start`, `turn/start`, or model work occurs. It binds source SHA-256 `8a5a093c9f6129e3a6d9fa50a3f0e8ba308ac9c2d656415c21dd1fe1ddbe8fae` and static-basis SHA-256 `f4a9e290cb1d0fb1bf5e72d9845801c62cba0f27bf9ef8415e6395fdbc47fd04` without retaining raw account data, email, quota values, credentials, or private paths. The one coherent final publisher, post-gates, and both mandatory fresh exact-tree reviews remain required.
- Revision 63 documentation freeze — 2026-07-21: the first coherent final publisher passes all 13 required deterministic and protected live rows and atomically stages a green pair on tree `6cd2571c8b0d7c4e6a5d76df99844776e074f625`. Readiness SHA-256 `ce5f06abd819d04c0a152269cba763f1758b2075dbc51c6bb726f14fb0adf079` and schema-manifest SHA-256 `1001d06ab5557ff35eb1207d2b06443e4c876c2c1e293f0e178a2eec9b90e4e8` independently verify; runtime/package pass, blockers are empty, installed `0.144.3` verifies 1,873 schema files, the production smoke sends only initialize/initialized and starts no model work, hygiene passes, and two final 2,110-entry archives are byte-identical at SHA-256 `87b6feb4f517b0f454fff01cc7417d7ad5fd2887564bbcff8ac9409c144f0932`. The protected Linear row again executes exactly nine fixed queries and zero mutations. Recording that result intentionally invalidates the pair as final evidence. Exactly one documentation-frozen publisher and affected post-gates must repeat before the two mandatory exact-tree reviews; no further full replay is permitted without a material finding.
- Revision 64 — 2026-07-21: both fresh reviews independently rejected the documentation-frozen Revision 63 tree `1faeb93318a9714963fca79a2e6142e73cc9120e` with the same P1: an internally consistent but release-blocked pair was staged and reported published. Readiness SHA-256 `2462920a2339fdc2a63a382ffcddc782966602cfaff52273300fb1db4f756b2a` and schema-manifest SHA-256 `af33a640f41f9eb6be33c1247b881dc8c94d7b85499a2f1ced4bcf719fe583e2` recorded 11/13 passing rows, blocked `installed_codex_verify` and `upstream_make_all`, 351 blockers, runtime `blocked`/`blocked_r0_06`, blocked platform OS status, and passing package status. The two blocked rows arose because their prepublication checks consulted the superseded pair that the publisher needed to replace; exact private installed-Codex and complete-gate diagnostics independently passed. Revision 64 adds a dedicated source-bound prepublication command that never consults that pair and requires release acceptance before compiler return, before and under publication, and during final canonical verification. Acceptance means the exact 13-row required inventory all passes, blockers are empty, runtime/platform/package status passes, and paired-schema runtime/overall status passes. Blocked pairs remain diagnostic only. Targeted regressions and exact 157-test discovery pass; final reseal, one repaired publisher, affected post-gates, and two fresh reviews remain pending.
- Revision 65 — 2026-07-21: Revision 64 was resealed at exact staged tree `77baaf6bdc182c148b180afee81946e4a34f3ad8`; its source-bound oracle passed 549/549 and the dedicated prepublication verifier passed installed Codex `0.144.3`, all 1,873 schema files, and bundle SHA-256 `d96f8d427cf68655b5658ebea0e9e2332986e8b4b9c1f0ff078957e2ceb70d69`. The single repaired publisher passed the exact 157-test harness and deterministic regeneration, constructed the private runtime, and entered the full gate before failing closed because its final semantic Git-metadata fingerprint no longer matched the original. It published no candidate pair and returned no success. Concurrent read-only release and preview audits issued Git reads against the canonical worktree during the sealed interval, but the aggregate rejection does not retain which fingerprint component changed and a later status-refresh experiment did not reproduce semantic drift; no narrower cause is claimed. No protected capability result was accepted, no Linear mutation occurred, and neither fixture changed. The exact tree, HEAD, refs, and staged semantics remain preserved. Revision 65 changes evidence only, requires no agents or Git/worktree readers during the next sealed publisher, and permits one freshly resealed exclusive retry followed by affected post-gates and two fresh independent exact-tree reviews.
- Revision 66 — 2026-07-21: the Revision 65 publisher preserved the exact semantic Git-metadata fence and passed every earlier required row, but returned only `upstream_make_all` blocked after 42m53.916s while parallel external agents and host workloads were active. The preceding Revision 64 attempt lasted 42m56.457s; subtracting the row's exact 1,800-second child deadline leaves the same approximately 12m54s pre-row path. The failed run prepared no transaction, published no pair, accepted no protected result, executed no Linear mutation, and changed neither fixture. A credential-free diagnostic reconstructed the exact staged snapshot and private sandbox and ran only the final row with an extended diagnostic ceiling. It passed in 1,248.048 seconds with return code zero, 660 tests, 100% measured coverage, clean Credo, zero Dialyzer errors, no error marker, complete metadata-safe and filename/version-consistent PLTs at fingerprint `acb47d247bdf8e037c5673ff12df3cda3863dea7899e25d3b38739c1f7dc4f38`, a 15.422-second byte-stable offline Dialyzer replay, complete generated outputs at digest `ca243a02fea4b27ba84ac51e362f5d4d0e3dc6bbb0b3fe432d1d516bd7128c33`, and every remaining post-gate integrity oracle. The original audited bound remains valid when release validation owns the host resources; no source repair or bound inflation is justified. Revision 66 changes evidence only and permits one freshly resealed publisher with no agents, diagnostics, Git readers, or parallel host workloads, followed by affected post-gates and two fresh independent exact-tree reviews.
- Revision 67 — 2026-07-21: R0-06 is accepted and pushed at commit `25f4d78e1eb3102dd8d1aa72413988b36b72fd71`; R0-07 begins from that exact foundation. Candidate-local release tooling implements external canonical manifests, protected dispatch and merge proofs, deterministic packaging, unprivileged clean-install/real-runner smoke, progressive atomic publication receipts, custom publication attestation, exact remote asset verification, and upstream-baseline return. Pre-acceptance audits repaired receipt loss, future-time causality, protected-main retry, manifest attachment, version-only smoke, dispatch ordering, already-merged retry reachability, safe-revert identity/authentication/TOCTOU behavior, source-integrity classification, and workflow provenance. Current targeted evidence is 38/38 Python release-tool tests, 9/9 CLI tests, public specs, format, strict Credo, and checksum-verified actionlint. The earlier archive rehearsal is superseded. A current independent-build rehearsal failed closed because pinned dependency compile-time map/AST ordering remained nondeterministic under OTP 28; exact build-only dependency patches are under independent proof. A comprehensive lock-bound runtime SBOM/license-notice inventory is also required before acceptance. The final exact-tree complete gate, current package/real-runner/baseline rehearsal, two fresh independent reviews, dedicated App configuration, protected PR/merge, immutable publication, and downloaded-asset verification remain pending. No tag, release, protected-main merge, or R0-07 acceptance is claimed.
- Revision 68 — 2026-07-21: R0-07 replaces the rejected repository-secret/automatic-revert direction with a reviewed out-of-band trusted CheckRun attestor, public source digest `fd204268d41548ef4fc419e1c799ecd95c35a723d56d46213900359cc48a93fa`, owner-only external seal, exact single-repository/minimum-permission App validation, independently approved workflow/review/gate hashes, live PR/base/tree/check/workflow/artifact revalidation, and at most one idempotent CheckRun mutation. The App private key and installation token never enter repository Actions; post-merge failure remains visibly action-required. Exact build-only dependency patches reproduce byte-identical package inputs, and the lock-bound runtime closure, notices, package inventory, and SPDX SBOM pass targeted validation. Current targeted evidence is 45/45 release-tool tests, 22/22 trusted-attestor tests, 10/10 CLI tests, public specs, `git diff --check`, and checksum-verified actionlint 1.7.12 over all four workflows. The current package/clean-install rehearsal, complete exact-tree gate, two reviews, dedicated App configuration, protected PR/merge, immutable publication, and downloaded-asset verification remain pending. No tag, PR, release, or R0-07 acceptance is claimed.
- Revision 69 — 2026-07-21: the emergency deadline override creates the honestly labelled `release/v0.2.0-buildweek-preview.1` branch from accepted R0-06 and integrates the isolated intent, interface, and verification workstreams. The production LiveView routes share one canonical Intent Service, use an exact-mutation separately credentialed Linear broker, preserve normal Symphony admission, and project real process-local runtime events into Mission Control and Run Detail. Launch/reset, browser contracts, public audit, and judge guidance are present. Commit `fd9cd006949facc7ba1e9cb5dc99ae9130e681ad` is publicly owner-testable in read-only mode with 94 integrated Elixir tests, 20 CLI tests, four browser-oracle controls, a 2,177-file clean public audit, desktop/mobile route evidence with no console/page/Axe failure, and a five-task production proposal with zero Linear mutation. No demo issue, live write, real Codex run, completed golden path, or final preview acceptance is claimed.
- Revision 70 — 2026-07-21: a fresh exact-commit review rejects Revision 69 with one P1 false-completion path and acceptance-impacting P2 findings. The repair isolates proof to the latest admitted attempt, requires contiguous replay and explicit success, rejects unresolved findings, binds checks/review/evidence/delivery/handoff/terminal state to one immutable revision, separates requested from runtime-attested model/effort, and keeps completion blocked without current-attempt GPT-5.6 Sol Ultra/Ultra evidence. The verification schema, state vocabulary, handoff timing, and source documentation now match production. MCP is reduced to five non-mutating liaison tools; only the trusted local UI can approve, publish, or start. The protected write loader requires a simultaneously available R0 comparison authority and rejects unavailable or equal values before its callback using fixed-size constant-time comparison. Affected evidence passes 51 Elixir tests, 20 CLI tests, four browser-oracle tests, warnings-as-errors compile, public specs, strict Credo, skill validation, `git diff --check`, and a 2,177-file public audit. No credential was inspected, no Linear mutation ran, and neither fixture changed. Source-bound reseal, exact regenerate comparison, one final complete candidate gate, exact-commit clean launch/browser audit, and replacement fresh review remain required.
